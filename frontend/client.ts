// Code generated by the Encore 1.53.2 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/
import type { CookieWithOptions } from "encore.dev/api";

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the  Encore application.
 */
export class Client {
    public readonly absence: absence.ServiceClient
    public readonly admin: admin.ServiceClient
    public readonly calendar: calendar.ServiceClient
    public readonly crew: crew.ServiceClient
    public readonly dictionary: dictionary.ServiceClient
    public readonly inspection: inspection.ServiceClient
    public readonly mock: mock.ServiceClient
    public readonly policy: policy.ServiceClient
    public readonly profile: profile.ServiceClient
    public readonly report: report.ServiceClient
    public readonly settings: settings.ServiceClient
    public readonly skill: skill.ServiceClient
    public readonly targets: targets.ServiceClient
    public readonly task: task.ServiceClient
    public readonly user: user.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.absence = new absence.ServiceClient(base)
        this.admin = new admin.ServiceClient(base)
        this.calendar = new calendar.ServiceClient(base)
        this.crew = new crew.ServiceClient(base)
        this.dictionary = new dictionary.ServiceClient(base)
        this.inspection = new inspection.ServiceClient(base)
        this.mock = new mock.ServiceClient(base)
        this.policy = new policy.ServiceClient(base)
        this.profile = new profile.ServiceClient(base)
        this.report = new report.ServiceClient(base)
        this.settings = new settings.ServiceClient(base)
        this.skill = new skill.ServiceClient(base)
        this.targets = new targets.ServiceClient(base)
        this.task = new task.ServiceClient(base)
        this.user = new user.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * Import the auth handler to be able to derive the auth type
 */
import type { auth as auth_auth } from "~backend/auth/auth";

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    /**
     * Allows you to set the authentication data to be used for each
     * request either by passing in a static object or by passing in
     * a function which returns a new object for each request.
     */
    auth?: RequestType<typeof auth_auth> | AuthDataGenerator
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { approve as api_absence_approve_approve } from "~backend/absence/approve";
import { create as api_absence_create_create } from "~backend/absence/create";
import { getStats as api_absence_get_stats_getStats } from "~backend/absence/get_stats";
import { list as api_absence_list_list } from "~backend/absence/list";

export namespace absence {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.approve = this.approve.bind(this)
            this.create = this.create.bind(this)
            this.getStats = this.getStats.bind(this)
            this.list = this.list.bind(this)
        }

        public async approve(params: { id: number }): Promise<ResponseType<typeof api_absence_approve_approve>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/absences/${encodeURIComponent(params.id)}/approve`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_absence_approve_approve>
        }

        public async create(params: RequestType<typeof api_absence_create_create>): Promise<ResponseType<typeof api_absence_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/absences`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_absence_create_create>
        }

        public async getStats(params: { user_id: string }): Promise<ResponseType<typeof api_absence_get_stats_getStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/absences/stats/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_absence_get_stats_getStats>
        }

        public async list(params: RequestType<typeof api_absence_list_list>): Promise<ResponseType<typeof api_absence_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"],
                limit:        params.limit === undefined ? undefined : String(params.limit),
                offset:       params.offset === undefined ? undefined : String(params.offset),
                "start_date": params["start_date"],
                status:       params.status === undefined ? undefined : String(params.status),
                "user_id":    params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/absences`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_absence_list_list>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { bootstrap as api_admin_bootstrap_bootstrap } from "~backend/admin/bootstrap";
import { changeRole as api_admin_change_role_changeRole } from "~backend/admin/change_role";
import { checkWC as api_admin_check_wc_checkWC } from "~backend/admin/check_wc";
import { createActivityLog as api_admin_create_activity_log_createActivityLog } from "~backend/admin/create_activity_log";
import { deactivateUser as api_admin_deactivate_user_deactivateUser } from "~backend/admin/deactivate_user";
import { getActivityLog as api_admin_get_activity_log_getActivityLog } from "~backend/admin/get_activity_log";
import { inviteUser as api_admin_invite_user_inviteUser } from "~backend/admin/invite_user";
import { listUsers as api_admin_list_users_listUsers } from "~backend/admin/list_users";
import { reactivateUser as api_admin_reactivate_user_reactivateUser } from "~backend/admin/reactivate_user";
import {
    getReassignmentPreview as api_admin_reassign_assets_getReassignmentPreview,
    reassignAssets as api_admin_reassign_assets_reassignAssets
} from "~backend/admin/reassign_assets";

export namespace admin {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.bootstrap = this.bootstrap.bind(this)
            this.changeRole = this.changeRole.bind(this)
            this.checkWC = this.checkWC.bind(this)
            this.createActivityLog = this.createActivityLog.bind(this)
            this.deactivateUser = this.deactivateUser.bind(this)
            this.getActivityLog = this.getActivityLog.bind(this)
            this.getReassignmentPreview = this.getReassignmentPreview.bind(this)
            this.inviteUser = this.inviteUser.bind(this)
            this.listUsers = this.listUsers.bind(this)
            this.reactivateUser = this.reactivateUser.bind(this)
            this.reassignAssets = this.reassignAssets.bind(this)
        }

        public async bootstrap(params: RequestType<typeof api_admin_bootstrap_bootstrap>): Promise<ResponseType<typeof api_admin_bootstrap_bootstrap>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                setupToken: params.setupToken,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/bootstrap`, {query, method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_bootstrap_bootstrap>
        }

        public async changeRole(params: RequestType<typeof api_admin_change_role_changeRole>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                newRole: params.newRole,
            }

            await this.baseClient.callTypedAPI(`/api/admin/users/${encodeURIComponent(params.userId)}/role`, {method: "PUT", body: JSON.stringify(body)})
        }

        public async checkWC(): Promise<ResponseType<typeof api_admin_check_wc_checkWC>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/check-wc`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_check_wc_checkWC>
        }

        public async createActivityLog(params: RequestType<typeof api_admin_create_activity_log_createActivityLog>): Promise<ResponseType<typeof api_admin_create_activity_log_createActivityLog>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/activity-log`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_create_activity_log_createActivityLog>
        }

        public async deactivateUser(params: RequestType<typeof api_admin_deactivate_user_deactivateUser>): Promise<void> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                deactivationDate: params.deactivationDate,
                reason:           params.reason,
            }

            await this.baseClient.callTypedAPI(`/api/admin/users/${encodeURIComponent(params.userId)}/deactivate`, {method: "POST", body: JSON.stringify(body)})
        }

        public async getActivityLog(params: RequestType<typeof api_admin_get_activity_log_getActivityLog>): Promise<ResponseType<typeof api_admin_get_activity_log_getActivityLog>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "entity_type": params["entity_type"],
                limit:         params.limit === undefined ? undefined : String(params.limit),
                offset:        params.offset === undefined ? undefined : String(params.offset),
                "user_id":     params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/admin/activity-log`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_get_activity_log_getActivityLog>
        }

        public async getReassignmentPreview(params: { userId: string }): Promise<ResponseType<typeof api_admin_reassign_assets_getReassignmentPreview>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/admin/reassign/${encodeURIComponent(params.userId)}/preview`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_reassign_assets_getReassignmentPreview>
        }

        public async inviteUser(params: RequestType<typeof api_admin_invite_user_inviteUser>): Promise<ResponseType<typeof api_admin_invite_user_inviteUser>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/admin/invite`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_invite_user_inviteUser>
        }

        public async listUsers(): Promise<ResponseType<typeof api_admin_list_users_listUsers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/admin/users`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_admin_list_users_listUsers>
        }

        public async reactivateUser(params: { userId: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/api/admin/users/${encodeURIComponent(params.userId)}/reactivate`, {method: "POST", body: undefined})
        }

        public async reassignAssets(params: RequestType<typeof api_admin_reassign_assets_reassignAssets>): Promise<void> {
            await this.baseClient.callTypedAPI(`/api/admin/reassign`, {method: "POST", body: JSON.stringify(params)})
        }
    }
}


export namespace auth {
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_calendar_create_create } from "~backend/calendar/create";
import { deleteEvent as api_calendar_delete_deleteEvent } from "~backend/calendar/delete";
import { exportICS as api_calendar_export_ics_exportICS } from "~backend/calendar/export_ics";
import { list as api_calendar_list_list } from "~backend/calendar/list";
import { update as api_calendar_update_update } from "~backend/calendar/update";

export namespace calendar {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteEvent = this.deleteEvent.bind(this)
            this.exportICS = this.exportICS.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_calendar_create_create>): Promise<ResponseType<typeof api_calendar_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/calendar/events`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_calendar_create_create>
        }

        public async deleteEvent(params: RequestType<typeof api_calendar_delete_deleteEvent>): Promise<void> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            await this.baseClient.callTypedAPI(`/calendar/events/${encodeURIComponent(params.id)}`, {query, method: "DELETE", body: undefined})
        }

        public async exportICS(params: RequestType<typeof api_calendar_export_ics_exportICS>): Promise<ResponseType<typeof api_calendar_export_ics_exportICS>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"],
                "start_date": params["start_date"],
                "user_id":    params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/calendar/export.ics`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_calendar_export_ics_exportICS>
        }

        public async list(params: RequestType<typeof api_calendar_list_list>): Promise<ResponseType<typeof api_calendar_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":       params["end_date"],
                "is_watch_event": params["is_watch_event"] === undefined ? undefined : String(params["is_watch_event"]),
                limit:            params.limit === undefined ? undefined : String(params.limit),
                offset:           params.offset === undefined ? undefined : String(params.offset),
                "start_date":     params["start_date"],
                "user_id":        params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/calendar/events`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_calendar_list_list>
        }

        public async update(params: RequestType<typeof api_calendar_update_update>): Promise<ResponseType<typeof api_calendar_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                updates:   params.updates,
                "user_id": params["user_id"],
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/calendar/events/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_calendar_update_update>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { bulkAssign as api_crew_bulk_assign_bulkAssign } from "~backend/crew/bulk_assign";
import { getStats as api_crew_get_stats_getStats } from "~backend/crew/get_stats";

export namespace crew {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.bulkAssign = this.bulkAssign.bind(this)
            this.getStats = this.getStats.bind(this)
        }

        public async bulkAssign(params: RequestType<typeof api_crew_bulk_assign_bulkAssign>): Promise<ResponseType<typeof api_crew_bulk_assign_bulkAssign>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/crew/bulk-assign`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_crew_bulk_assign_bulkAssign>
        }

        public async getStats(): Promise<ResponseType<typeof api_crew_get_stats_getStats>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/crew/stats`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_crew_get_stats_getStats>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_dictionary_create_create } from "~backend/dictionary/create";
import { deleteDictionary as api_dictionary_delete_deleteDictionary } from "~backend/dictionary/delete";
import { list as api_dictionary_list_list } from "~backend/dictionary/list";
import { update as api_dictionary_update_update } from "~backend/dictionary/update";

export namespace dictionary {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteDictionary = this.deleteDictionary.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_dictionary_create_create>): Promise<ResponseType<typeof api_dictionary_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/dictionary`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_dictionary_create_create>
        }

        public async deleteDictionary(params: { id: number }): Promise<ResponseType<typeof api_dictionary_delete_deleteDictionary>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/dictionary/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_dictionary_delete_deleteDictionary>
        }

        public async list(params: RequestType<typeof api_dictionary_list_list>): Promise<ResponseType<typeof api_dictionary_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                active: params.active === undefined ? undefined : String(params.active),
                type:   params.type === undefined ? undefined : String(params.type),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/dictionary`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_dictionary_list_list>
        }

        public async update(params: RequestType<typeof api_dictionary_update_update>): Promise<ResponseType<typeof api_dictionary_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                active: params.active,
                value:  params.value,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/dictionary/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_dictionary_update_update>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_inspection_create_create } from "~backend/inspection/create";
import { list as api_inspection_list_list } from "~backend/inspection/list";
import { update as api_inspection_update_update } from "~backend/inspection/update";

export namespace inspection {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_inspection_create_create>): Promise<ResponseType<typeof api_inspection_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/inspections`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_inspection_create_create>
        }

        public async list(params: RequestType<typeof api_inspection_list_list>): Promise<ResponseType<typeof api_inspection_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "inspection_type": params["inspection_type"],
                "inspector_id":    params["inspector_id"],
                limit:             params.limit === undefined ? undefined : String(params.limit),
                offset:            params.offset === undefined ? undefined : String(params.offset),
                status:            params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/inspections`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_inspection_list_list>
        }

        public async update(params: RequestType<typeof api_inspection_update_update>): Promise<ResponseType<typeof api_inspection_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                address:             params.address,
                "assigned_crew_ids": params["assigned_crew_ids"],
                "completed_date":    params["completed_date"],
                notes:               params.notes,
                priority:            params.priority,
                "scheduled_for":     params["scheduled_for"],
                status:              params.status,
                type:                params.type,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/inspections/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_inspection_update_update>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import {
    createMockAbsence as api_mock_api_absences_createMockAbsence,
    getMockAbsences as api_mock_api_absences_getMockAbsences
} from "~backend/mock/api_absences";
import {
    createSkill as api_mock_api_dictionaries_createSkill,
    getSkills as api_mock_api_dictionaries_getSkills
} from "~backend/mock/api_dictionaries";
import {
    getMockPeople as api_mock_api_people_getMockPeople,
    getMockPerson as api_mock_api_people_getMockPerson,
    updateMockPerson as api_mock_api_people_updateMockPerson
} from "~backend/mock/api_people";
import {
    getAbsenceTriggers as api_mock_api_settings_getAbsenceTriggers,
    updateAbsenceTriggers as api_mock_api_settings_updateAbsenceTriggers
} from "~backend/mock/api_settings";
import {
    getMockInspection as api_mock_inspections_getMockInspection,
    getMockInspections as api_mock_inspections_getMockInspections
} from "~backend/mock/inspections";
import {
    getMockPolicyDocs as api_mock_policies_getMockPolicyDocs,
    getMockPolicyQueries as api_mock_policies_getMockPolicyQueries,
    mockAskPolicy as api_mock_policies_mockAskPolicy
} from "~backend/mock/policies";
import {
    getMockTarget as api_mock_targets_getMockTarget,
    getMockTargets as api_mock_targets_getMockTargets
} from "~backend/mock/targets";
import {
    getMockTask as api_mock_tasks_getMockTask,
    getMockTasks as api_mock_tasks_getMockTasks
} from "~backend/mock/tasks";
import {
    getMockUser as api_mock_users_getMockUser,
    getMockUsers as api_mock_users_getMockUsers
} from "~backend/mock/users";

export namespace mock {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createMockAbsence = this.createMockAbsence.bind(this)
            this.createSkill = this.createSkill.bind(this)
            this.getAbsenceTriggers = this.getAbsenceTriggers.bind(this)
            this.getMockAbsences = this.getMockAbsences.bind(this)
            this.getMockInspection = this.getMockInspection.bind(this)
            this.getMockInspections = this.getMockInspections.bind(this)
            this.getMockPeople = this.getMockPeople.bind(this)
            this.getMockPerson = this.getMockPerson.bind(this)
            this.getMockPolicyDocs = this.getMockPolicyDocs.bind(this)
            this.getMockPolicyQueries = this.getMockPolicyQueries.bind(this)
            this.getMockTarget = this.getMockTarget.bind(this)
            this.getMockTargets = this.getMockTargets.bind(this)
            this.getMockTask = this.getMockTask.bind(this)
            this.getMockTasks = this.getMockTasks.bind(this)
            this.getMockUser = this.getMockUser.bind(this)
            this.getMockUsers = this.getMockUsers.bind(this)
            this.getSkills = this.getSkills.bind(this)
            this.mockAskPolicy = this.mockAskPolicy.bind(this)
            this.updateAbsenceTriggers = this.updateAbsenceTriggers.bind(this)
            this.updateMockPerson = this.updateMockPerson.bind(this)
        }

        public async createMockAbsence(params: RequestType<typeof api_mock_api_absences_createMockAbsence>): Promise<ResponseType<typeof api_mock_api_absences_createMockAbsence>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                createdByUserId: params.createdByUserId,
                endDate:         params.endDate,
                reason:          params.reason,
                startDate:       params.startDate,
                totalDays:       params.totalDays,
                type:            params.type,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/people/${encodeURIComponent(params.id)}/absences`, {method: "POST", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_absences_createMockAbsence>
        }

        public async createSkill(params: RequestType<typeof api_mock_api_dictionaries_createSkill>): Promise<ResponseType<typeof api_mock_api_dictionaries_createSkill>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/dictionaries/skills`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_dictionaries_createSkill>
        }

        public async getAbsenceTriggers(): Promise<ResponseType<typeof api_mock_api_settings_getAbsenceTriggers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/settings/absence-triggers`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_settings_getAbsenceTriggers>
        }

        public async getMockAbsences(params: { id: string }): Promise<ResponseType<typeof api_mock_api_absences_getMockAbsences>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/people/${encodeURIComponent(params.id)}/absences`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_absences_getMockAbsences>
        }

        public async getMockInspection(params: { id: number }): Promise<ResponseType<typeof api_mock_inspections_getMockInspection>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/inspections/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_inspections_getMockInspection>
        }

        public async getMockInspections(params: RequestType<typeof api_mock_inspections_getMockInspections>): Promise<ResponseType<typeof api_mock_inspections_getMockInspections>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                status: params.status,
                type:   params.type,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/inspections`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_inspections_getMockInspections>
        }

        public async getMockPeople(): Promise<ResponseType<typeof api_mock_api_people_getMockPeople>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/people`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_people_getMockPeople>
        }

        public async getMockPerson(params: { id: string }): Promise<ResponseType<typeof api_mock_api_people_getMockPerson>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/people/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_people_getMockPerson>
        }

        public async getMockPolicyDocs(params: RequestType<typeof api_mock_policies_getMockPolicyDocs>): Promise<ResponseType<typeof api_mock_policies_getMockPolicyDocs>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category: params.category,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/policies/docs`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_policies_getMockPolicyDocs>
        }

        public async getMockPolicyQueries(params: RequestType<typeof api_mock_policies_getMockPolicyQueries>): Promise<ResponseType<typeof api_mock_policies_getMockPolicyQueries>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/policies/queries`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_policies_getMockPolicyQueries>
        }

        public async getMockTarget(params: { id: number }): Promise<ResponseType<typeof api_mock_targets_getMockTarget>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/targets/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_targets_getMockTarget>
        }

        public async getMockTargets(params: RequestType<typeof api_mock_targets_getMockTargets>): Promise<ResponseType<typeof api_mock_targets_getMockTargets>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                metric: params.metric,
                status: params.status,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/targets`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_targets_getMockTargets>
        }

        public async getMockTask(params: { id: number }): Promise<ResponseType<typeof api_mock_tasks_getMockTask>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/tasks/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_tasks_getMockTask>
        }

        public async getMockTasks(params: RequestType<typeof api_mock_tasks_getMockTasks>): Promise<ResponseType<typeof api_mock_tasks_getMockTasks>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "assigned_to": params["assigned_to"],
                status:        params.status,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/tasks`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_tasks_getMockTasks>
        }

        public async getMockUser(params: { id: string }): Promise<ResponseType<typeof api_mock_users_getMockUser>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/users/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_users_getMockUser>
        }

        public async getMockUsers(params: RequestType<typeof api_mock_users_getMockUsers>): Promise<ResponseType<typeof api_mock_users_getMockUsers>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                role: params.role,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/users`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_users_getMockUsers>
        }

        public async getSkills(): Promise<ResponseType<typeof api_mock_api_dictionaries_getSkills>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/dictionaries/skills`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_dictionaries_getSkills>
        }

        public async mockAskPolicy(params: RequestType<typeof api_mock_policies_mockAskPolicy>): Promise<ResponseType<typeof api_mock_policies_mockAskPolicy>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/mock/policies/ask`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_policies_mockAskPolicy>
        }

        public async updateAbsenceTriggers(params: RequestType<typeof api_mock_api_settings_updateAbsenceTriggers>): Promise<ResponseType<typeof api_mock_api_settings_updateAbsenceTriggers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/settings/absence-triggers`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_settings_updateAbsenceTriggers>
        }

        public async updateMockPerson(params: RequestType<typeof api_mock_api_people_updateMockPerson>): Promise<ResponseType<typeof api_mock_api_people_updateMockPerson>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                createdAt:        params.createdAt,
                customFields:     params.customFields,
                driverPathway:    params.driverPathway,
                email:            params.email,
                isActive:         params.isActive,
                lastConversation: params.lastConversation,
                name:             params.name,
                niNumber:         params.niNumber,
                notes:            params.notes,
                phone:            params.phone,
                rank:             params.rank,
                role:             params.role,
                skills:           params.skills,
                staffNumber:      params.staffNumber,
                updatedAt:        params.updatedAt,
                watch:            params.watch,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/api/people/${encodeURIComponent(params.id)}`, {method: "PUT", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_mock_api_people_updateMockPerson>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { ask as api_policy_ask_ask } from "~backend/policy/ask";
import { getDownloadUrl as api_policy_get_download_url_getDownloadUrl } from "~backend/policy/get_download_url";
import { getUploadUrl as api_policy_get_upload_url_getUploadUrl } from "~backend/policy/get_upload_url";
import { list as api_policy_list_list } from "~backend/policy/list";
import { processEmbedding as api_policy_process_embedding_processEmbedding } from "~backend/policy/process_embedding";
import { queryHistory as api_policy_query_history_queryHistory } from "~backend/policy/query_history";
import { upload as api_policy_upload_upload } from "~backend/policy/upload";

export namespace policy {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.ask = this.ask.bind(this)
            this.getDownloadUrl = this.getDownloadUrl.bind(this)
            this.getUploadUrl = this.getUploadUrl.bind(this)
            this.list = this.list.bind(this)
            this.processEmbedding = this.processEmbedding.bind(this)
            this.queryHistory = this.queryHistory.bind(this)
            this.upload = this.upload.bind(this)
        }

        public async ask(params: RequestType<typeof api_policy_ask_ask>): Promise<ResponseType<typeof api_policy_ask_ask>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/policies/ask`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_policy_ask_ask>
        }

        public async getDownloadUrl(params: { id: number }): Promise<ResponseType<typeof api_policy_get_download_url_getDownloadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/policies/${encodeURIComponent(params.id)}/download-url`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_policy_get_download_url_getDownloadUrl>
        }

        public async getUploadUrl(params: RequestType<typeof api_policy_get_upload_url_getUploadUrl>): Promise<ResponseType<typeof api_policy_get_upload_url_getUploadUrl>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/policies/upload-url`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_policy_get_upload_url_getUploadUrl>
        }

        public async list(params: RequestType<typeof api_policy_list_list>): Promise<ResponseType<typeof api_policy_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category: params.category,
                limit:    params.limit === undefined ? undefined : String(params.limit),
                offset:   params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/policies`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_policy_list_list>
        }

        public async processEmbedding(params: { policy_id: number }): Promise<ResponseType<typeof api_policy_process_embedding_processEmbedding>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/policies/${encodeURIComponent(params.policy_id)}/embed`, {method: "POST", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_policy_process_embedding_processEmbedding>
        }

        public async queryHistory(params: RequestType<typeof api_policy_query_history_queryHistory>): Promise<ResponseType<typeof api_policy_query_history_queryHistory>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:     params.limit === undefined ? undefined : String(params.limit),
                offset:    params.offset === undefined ? undefined : String(params.offset),
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/policies/queries`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_policy_query_history_queryHistory>
        }

        public async upload(params: RequestType<typeof api_policy_upload_upload>): Promise<ResponseType<typeof api_policy_upload_upload>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/policies`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_policy_upload_upload>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_profile_create_create } from "~backend/profile/create";
import { createPerson as api_profile_create_person_createPerson } from "~backend/profile/create_person";
import { getByUser as api_profile_get_by_user_getByUser } from "~backend/profile/get_by_user";
import { list as api_profile_list_list } from "~backend/profile/list";
import { listWithUsers as api_profile_list_with_users_listWithUsers } from "~backend/profile/list_with_users";
import { recalculateTriggers as api_profile_recalculate_triggers_recalculateTriggers } from "~backend/profile/recalculate_triggers";
import { update as api_profile_update_update } from "~backend/profile/update";

export namespace profile {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.createPerson = this.createPerson.bind(this)
            this.getByUser = this.getByUser.bind(this)
            this.list = this.list.bind(this)
            this.listWithUsers = this.listWithUsers.bind(this)
            this.recalculateTriggers = this.recalculateTriggers.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_profile_create_create>): Promise<ResponseType<typeof api_profile_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profiles`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_create_create>
        }

        public async createPerson(params: RequestType<typeof api_profile_create_person_createPerson>): Promise<ResponseType<typeof api_profile_create_person_createPerson>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/people`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_create_person_createPerson>
        }

        public async getByUser(params: { user_id: string }): Promise<ResponseType<typeof api_profile_get_by_user_getByUser>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profiles/user/${encodeURIComponent(params.user_id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_get_by_user_getByUser>
        }

        public async list(params: RequestType<typeof api_profile_list_list>): Promise<ResponseType<typeof api_profile_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:   params.limit === undefined ? undefined : String(params.limit),
                offset:  params.offset === undefined ? undefined : String(params.offset),
                shift:   params.shift,
                station: params.station,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profiles`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_list_list>
        }

        public async listWithUsers(params: RequestType<typeof api_profile_list_with_users_listWithUsers>): Promise<ResponseType<typeof api_profile_list_with_users_listWithUsers>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:        params.limit === undefined ? undefined : String(params.limit),
                offset:       params.offset === undefined ? undefined : String(params.offset),
                rank:         params.rank,
                shift:        params.shift,
                station:      params.station,
                status:       params.status === undefined ? undefined : String(params.status),
                "watch_unit": params["watch_unit"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/people`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_list_with_users_listWithUsers>
        }

        public async recalculateTriggers(params: RequestType<typeof api_profile_recalculate_triggers_recalculateTriggers>): Promise<ResponseType<typeof api_profile_recalculate_triggers_recalculateTriggers>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profile/recalculate-triggers`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_recalculate_triggers_recalculateTriggers>
        }

        public async update(params: RequestType<typeof api_profile_update_update>): Promise<ResponseType<typeof api_profile_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                ba:                        params.ba,
                certifications:            params.certifications,
                customFields:              params.customFields,
                driver:                    params.driver,
                driverPathway:             params.driverPathway,
                "emergency_contact_name":  params["emergency_contact_name"],
                "emergency_contact_phone": params["emergency_contact_phone"],
                "hire_date":               params["hire_date"],
                lastConversation:          params.lastConversation,
                "last_one_to_one_date":    params["last_one_to_one_date"],
                "next_one_to_one_date":    params["next_one_to_one_date"],
                notes:                     params.notes,
                phone:                     params.phone,
                prps:                      params.prps,
                rank:                      params.rank,
                "rolling_sick_days":       params["rolling_sick_days"],
                "rolling_sick_episodes":   params["rolling_sick_episodes"],
                "service_number":          params["service_number"],
                shift:                     params.shift,
                skills:                    params.skills,
                station:                   params.station,
                "trigger_stage":           params["trigger_stage"],
                watch:                     params.watch,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/profiles/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_profile_update_update>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { exportCsv as api_report_export_csv_exportCsv } from "~backend/report/export_csv";
import { exportTableCSV as api_report_export_table_csv_exportTableCSV } from "~backend/report/export_table_csv";
import { importStaffCsv as api_report_import_staff_csv_importStaffCsv } from "~backend/report/import_staff_csv";

export namespace report {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.exportCsv = this.exportCsv.bind(this)
            this.exportTableCSV = this.exportTableCSV.bind(this)
            this.importStaffCsv = this.importStaffCsv.bind(this)
        }

        public async exportCsv(params: RequestType<typeof api_report_export_csv_exportCsv>): Promise<ResponseType<typeof api_report_export_csv_exportCsv>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/reports/export/${encodeURIComponent(params.table)}`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_report_export_csv_exportCsv>
        }

        public async exportTableCSV(params: RequestType<typeof api_report_export_table_csv_exportTableCSV>): Promise<ResponseType<typeof api_report_export_table_csv_exportTableCSV>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/report/export`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_report_export_table_csv_exportTableCSV>
        }

        public async importStaffCsv(params: RequestType<typeof api_report_import_staff_csv_importStaffCsv>): Promise<ResponseType<typeof api_report_import_staff_csv_importStaffCsv>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/reports/import-staff`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_report_import_staff_csv_importStaffCsv>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { get as api_settings_get_get } from "~backend/settings/get";
import { updateAbsenceThresholds as api_settings_update_absence_thresholds_updateAbsenceThresholds } from "~backend/settings/update_absence_thresholds";
import { updateBranding as api_settings_update_branding_updateBranding } from "~backend/settings/update_branding";
import { updateSkillsCerts as api_settings_update_skills_certs_updateSkillsCerts } from "~backend/settings/update_skills_certs";
import { updateTriggerThresholds as api_settings_update_trigger_thresholds_updateTriggerThresholds } from "~backend/settings/update_trigger_thresholds";

export namespace settings {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.get = this.get.bind(this)
            this.updateAbsenceThresholds = this.updateAbsenceThresholds.bind(this)
            this.updateBranding = this.updateBranding.bind(this)
            this.updateSkillsCerts = this.updateSkillsCerts.bind(this)
            this.updateTriggerThresholds = this.updateTriggerThresholds.bind(this)
        }

        public async get(): Promise<ResponseType<typeof api_settings_get_get>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/settings`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_settings_get_get>
        }

        public async updateAbsenceThresholds(params: RequestType<typeof api_settings_update_absence_thresholds_updateAbsenceThresholds>): Promise<ResponseType<typeof api_settings_update_absence_thresholds_updateAbsenceThresholds>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/settings/absence-thresholds`, {method: "PATCH", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_settings_update_absence_thresholds_updateAbsenceThresholds>
        }

        public async updateBranding(params: RequestType<typeof api_settings_update_branding_updateBranding>): Promise<ResponseType<typeof api_settings_update_branding_updateBranding>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/settings/branding`, {method: "PATCH", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_settings_update_branding_updateBranding>
        }

        public async updateSkillsCerts(params: RequestType<typeof api_settings_update_skills_certs_updateSkillsCerts>): Promise<ResponseType<typeof api_settings_update_skills_certs_updateSkillsCerts>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/settings/skills-certs`, {method: "PATCH", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_settings_update_skills_certs_updateSkillsCerts>
        }

        public async updateTriggerThresholds(params: RequestType<typeof api_settings_update_trigger_thresholds_updateTriggerThresholds>): Promise<ResponseType<typeof api_settings_update_trigger_thresholds_updateTriggerThresholds>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/settings/trigger-thresholds`, {method: "PUT", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_settings_update_trigger_thresholds_updateTriggerThresholds>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_skill_create_create } from "~backend/skill/create";
import { deleteSkillRenewal as api_skill_delete_deleteSkillRenewal } from "~backend/skill/delete";
import { list as api_skill_list_list } from "~backend/skill/list";
import { update as api_skill_update_update } from "~backend/skill/update";

export namespace skill {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteSkillRenewal = this.deleteSkillRenewal.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_skill_create_create>): Promise<ResponseType<typeof api_skill_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/skills/renewals`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_skill_create_create>
        }

        public async deleteSkillRenewal(params: { id: number }): Promise<ResponseType<typeof api_skill_delete_deleteSkillRenewal>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/skills/renewals/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_skill_delete_deleteSkillRenewal>
        }

        public async list(params: RequestType<typeof api_skill_list_list>): Promise<ResponseType<typeof api_skill_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "profile_id": String(params["profile_id"]),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/skills/renewals`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_skill_list_list>
        }

        public async update(params: RequestType<typeof api_skill_update_update>): Promise<ResponseType<typeof api_skill_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                "acquired_date": params["acquired_date"],
                "expiry_date":   params["expiry_date"],
                notes:           params.notes,
                "reminder_date": params["reminder_date"],
                "renewal_date":  params["renewal_date"],
                "skill_name":    params["skill_name"],
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/skills/renewals/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_skill_update_update>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_targets_create_create } from "~backend/targets/create";
import { list as api_targets_list_list } from "~backend/targets/list";
import { update as api_targets_update_update } from "~backend/targets/update";

export namespace targets {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_targets_create_create>): Promise<ResponseType<typeof api_targets_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/targets`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_targets_create_create>
        }

        public async list(params: RequestType<typeof api_targets_list_list>): Promise<ResponseType<typeof api_targets_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:          params.limit === undefined ? undefined : String(params.limit),
                metric:         params.metric === undefined ? undefined : String(params.metric),
                offset:         params.offset === undefined ? undefined : String(params.offset),
                "period_end":   params["period_end"],
                "period_start": params["period_start"],
                status:         params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/targets`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_targets_list_list>
        }

        public async update(params: RequestType<typeof api_targets_update_update>): Promise<ResponseType<typeof api_targets_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                "actual_count": params["actual_count"],
                metric:         params.metric,
                notes:          params.notes,
                "period_end":   params["period_end"],
                "period_start": params["period_start"],
                status:         params.status,
                "target_count": params["target_count"],
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/targets/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_targets_update_update>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_task_create_create } from "~backend/task/create";
import { deleteTask as api_task_delete_deleteTask } from "~backend/task/delete";
import { list as api_task_list_list } from "~backend/task/list";
import { update as api_task_update_update } from "~backend/task/update";

export namespace task {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteTask = this.deleteTask.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_task_create_create>): Promise<ResponseType<typeof api_task_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tasks`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_task_create_create>
        }

        public async deleteTask(params: { id: number }): Promise<void> {
            await this.baseClient.callTypedAPI(`/tasks/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
        }

        public async list(params: RequestType<typeof api_task_list_list>): Promise<ResponseType<typeof api_task_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "assigned_by": params["assigned_by"],
                "assigned_to": params["assigned_to"],
                limit:         params.limit === undefined ? undefined : String(params.limit),
                offset:        params.offset === undefined ? undefined : String(params.offset),
                priority:      params.priority === undefined ? undefined : String(params.priority),
                status:        params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tasks`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_task_list_list>
        }

        public async update(params: RequestType<typeof api_task_update_update>): Promise<ResponseType<typeof api_task_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                "assigned_to": params["assigned_to"],
                attachments:   params.attachments,
                category:      params.category,
                checklist:     params.checklist,
                description:   params.description,
                "due_date":    params["due_date"],
                priority:      params.priority,
                rrule:         params.rrule,
                status:        params.status,
                tags:          params.tags,
                title:         params.title,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/tasks/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_task_update_update>
        }
    }
}

/**
 * Import the endpoint handlers to derive the types for the client.
 */
import { create as api_user_create_create } from "~backend/user/create";
import { deleteUser as api_user_delete_deleteUser } from "~backend/user/delete";
import { get as api_user_get_get } from "~backend/user/get";
import { list as api_user_list_list } from "~backend/user/list";
import { update as api_user_update_update } from "~backend/user/update";

export namespace user {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteUser = this.deleteUser.bind(this)
            this.get = this.get.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: RequestType<typeof api_user_create_create>): Promise<ResponseType<typeof api_user_create_create>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/users`, {method: "POST", body: JSON.stringify(params)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_create_create>
        }

        public async deleteUser(params: { id: string }): Promise<void> {
            await this.baseClient.callTypedAPI(`/users/${encodeURIComponent(params.id)}`, {method: "DELETE", body: undefined})
        }

        public async get(params: { id: string }): Promise<ResponseType<typeof api_user_get_get>> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/users/${encodeURIComponent(params.id)}`, {method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_get_get>
        }

        public async list(params: RequestType<typeof api_user_list_list>): Promise<ResponseType<typeof api_user_list_list>> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
                role:   params.role === undefined ? undefined : String(params.role),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/users`, {query, method: "GET", body: undefined})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_list_list>
        }

        public async update(params: RequestType<typeof api_user_update_update>): Promise<ResponseType<typeof api_user_update_update>> {
            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                updates: params.updates,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI(`/users/${encodeURIComponent(params.id)}`, {method: "PATCH", body: JSON.stringify(body)})
            return JSON.parse(await resp.text(), dateReviver) as ResponseType<typeof api_user_update_update>
        }
    }
}


type PickMethods<Type> = Omit<CallParameters, "method"> & { method?: Type };

// Helper type to omit all fields that are cookies.
type OmitCookie<T> = {
  [K in keyof T as T[K] extends CookieWithOptions<any> ? never : K]: T[K];
};

type RequestType<Type extends (...args: any[]) => any> =
  Parameters<Type> extends [infer H, ...any[]]
    ? OmitCookie<H>
    : void;

type ResponseType<Type extends (...args: any[]) => any> = OmitCookie<Awaited<ReturnType<Type>>>;

function dateReviver(key: string, value: any): any {
  if (
    typeof value === "string" &&
    value.length >= 10 &&
    value.charCodeAt(0) >= 48 && // '0'
    value.charCodeAt(0) <= 57 // '9'
  ) {
    const parsedDate = new Date(value);
    if (!isNaN(parsedDate.getTime())) {
      return parsedDate;
    }
  }
  return value;
}


function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

import {
  StreamInOutHandlerFn,
  StreamInHandlerFn,
  StreamOutHandlerFn,
} from "encore.dev/api";

type StreamRequest<Type> = Type extends
  | StreamInOutHandlerFn<any, infer Req, any>
  | StreamInHandlerFn<any, infer Req, any>
  | StreamOutHandlerFn<any, any>
  ? Req
  : never;

type StreamResponse<Type> = Type extends
  | StreamInOutHandlerFn<any, any, infer Resp>
  | StreamInHandlerFn<any, any, infer Resp>
  | StreamOutHandlerFn<any, infer Resp>
  ? Resp
  : never;


function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data, dateReviver));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data, dateReviver))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
  | RequestType<typeof auth_auth>
  | Promise<RequestType<typeof auth_auth> | undefined>
  | undefined;

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
    readonly authGenerator?: AuthDataGenerator

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "-Generated-TS-Client (Encore/1.53.2)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }

        // Setup an authentication data generator using the auth data token option
        if (options.auth !== undefined) {
            const auth = options.auth
            if (typeof auth === "function") {
                this.authGenerator = auth
            } else {
                this.authGenerator = () => auth
            }
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        let authData: RequestType<typeof auth_auth> | undefined;

        // If authorization data generator is present, call it and add the returned data to the request
        if (this.authGenerator) {
            const mayBePromise = this.authGenerator();
            if (mayBePromise instanceof Promise) {
                authData = await mayBePromise;
            } else {
                authData = mayBePromise;
            }
        }

        if (authData) {
            const data: CallParameters = {};

            data.headers = makeRecord<string, string>({
                authorization: authData.authorization,
            });

            return data;
        }

        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(path: string, params?: CallParameters): Promise<Response> {
        return this.callAPI(path, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(path: string, params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}

export default new Client(import.meta.env.VITE_CLIENT_TARGET, { requestInit: { credentials: "include" } });
