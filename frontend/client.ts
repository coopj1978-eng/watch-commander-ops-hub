// Code generated by the Encore v1.54.0 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-watch-commander-ops-hub-8spi.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the watch-commander-ops-hub-8spi Encore application.
 */
export default class Client {
    public readonly absence: absence.ServiceClient
    public readonly activity: activity.ServiceClient
    public readonly admin: admin.ServiceClient
    public readonly appliance: appliance.ServiceClient
    public readonly calendar: calendar.ServiceClient
    public readonly crew: crew.ServiceClient
    public readonly crewing: crewing.ServiceClient
    public readonly detachments: detachments.ServiceClient
    public readonly dictionary: dictionary.ServiceClient
    public readonly frontend: frontend.ServiceClient
    public readonly h4h: h4h.ServiceClient
    public readonly handover: handover.ServiceClient
    public readonly inspection: inspection.ServiceClient
    public readonly inspection_plans: inspection_plans.ServiceClient
    public readonly localauth: localauth.ServiceClient
    public readonly mock: mock.ServiceClient
    public readonly note: note.ServiceClient
    public readonly notification: notification.ServiceClient
    public readonly policy: policy.ServiceClient
    public readonly profile: profile.ServiceClient
    public readonly quarterly_report: quarterly_report.ServiceClient
    public readonly report: report.ServiceClient
    public readonly settings: settings.ServiceClient
    public readonly shift_adjustments: shift_adjustments.ServiceClient
    public readonly skill: skill.ServiceClient
    public readonly targets: targets.ServiceClient
    public readonly task: task.ServiceClient
    public readonly user: user.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.absence = new absence.ServiceClient(base)
        this.activity = new activity.ServiceClient(base)
        this.admin = new admin.ServiceClient(base)
        this.appliance = new appliance.ServiceClient(base)
        this.calendar = new calendar.ServiceClient(base)
        this.crew = new crew.ServiceClient(base)
        this.crewing = new crewing.ServiceClient(base)
        this.detachments = new detachments.ServiceClient(base)
        this.dictionary = new dictionary.ServiceClient(base)
        this.frontend = new frontend.ServiceClient(base)
        this.h4h = new h4h.ServiceClient(base)
        this.handover = new handover.ServiceClient(base)
        this.inspection = new inspection.ServiceClient(base)
        this.inspection_plans = new inspection_plans.ServiceClient(base)
        this.localauth = new localauth.ServiceClient(base)
        this.mock = new mock.ServiceClient(base)
        this.note = new note.ServiceClient(base)
        this.notification = new notification.ServiceClient(base)
        this.policy = new policy.ServiceClient(base)
        this.profile = new profile.ServiceClient(base)
        this.quarterly_report = new quarterly_report.ServiceClient(base)
        this.report = new report.ServiceClient(base)
        this.settings = new settings.ServiceClient(base)
        this.shift_adjustments = new shift_adjustments.ServiceClient(base)
        this.skill = new skill.ServiceClient(base)
        this.targets = new targets.ServiceClient(base)
        this.task = new task.ServiceClient(base)
        this.user = new user.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    /**
     * Allows you to set the authentication data to be used for each
     * request either by passing in a static object or by passing in
     * a function which returns a new object for each request.
     */
    auth?: auth.AuthParams | AuthDataGenerator
}

export namespace absence {
    export interface Absence {
        id: number
        "firefighter_id": string
        type: AbsenceType
        "start_date": string
        "end_date": string
        "total_days"?: number
        reason: string
        docs?: string[]
        "evidence_urls"?: string[]
        "sick_line_document"?: string
        status: AbsenceStatus
        "approved_by"?: string
        "approved_at"?: string
        "created_by_user_id"?: string
        "created_at": string
        "updated_at": string
    }

    export interface AbsenceStats {
        "user_id": string
        "total_days": number
        "sick_days": number
        "vacation_days": number
        "other_days": number
        "six_month_total": number
        "stage_alert"?: string
    }

    export type AbsenceStatus = "pending" | "approved" | "rejected"

    export type AbsenceType = "sickness" | "AL" | "TOIL" | "parental" | "other"

    export interface CreateAbsenceRequest {
        "user_id": string
        type: AbsenceType
        "start_date": string
        "end_date": string
        reason: string
        "evidence_urls"?: string[]
    }

    export interface ListAbsencesRequest {
        "user_id"?: string
        status?: AbsenceStatus
        "start_date"?: string
        "end_date"?: string
        limit?: number
        offset?: number
    }

    export interface ListAbsencesResponse {
        absences: Absence[]
        total: number
    }

    export interface SelfReportRequest {
        "start_date": string
        "end_date": string
        reason?: string
        "sick_line_document"?: string
    }

    export interface TodaySickMember {
        "user_id": string
        name: string
        "watch_unit": string | null
    }

    export interface TodaySickResponse {
        "sick_staff": TodaySickMember[]
    }

    export interface UpdateAbsenceDatesRequest {
        "start_date"?: string
        "end_date"?: string
        "sick_line_document"?: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.approve = this.approve.bind(this)
            this.create = this.create.bind(this)
            this.getStats = this.getStats.bind(this)
            this.list = this.list.bind(this)
            this.selfReport = this.selfReport.bind(this)
            this.todaySick = this.todaySick.bind(this)
            this.updateAbsence = this.updateAbsence.bind(this)
        }

        public async approve(id: number): Promise<Absence> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/absences/${encodeURIComponent(id)}/approve`)
            return await resp.json() as Absence
        }

        public async create(params: CreateAbsenceRequest): Promise<Absence> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/absences`, JSON.stringify(params))
            return await resp.json() as Absence
        }

        public async getStats(user_id: string): Promise<AbsenceStats> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/absences/stats/${encodeURIComponent(user_id)}`)
            return await resp.json() as AbsenceStats
        }

        public async list(params: ListAbsencesRequest): Promise<ListAbsencesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"],
                limit:        params.limit === undefined ? undefined : String(params.limit),
                offset:       params.offset === undefined ? undefined : String(params.offset),
                "start_date": params["start_date"],
                status:       params.status === undefined ? undefined : String(params.status),
                "user_id":    params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/absences`, undefined, {query})
            return await resp.json() as ListAbsencesResponse
        }

        public async selfReport(params: SelfReportRequest): Promise<Absence> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/absences/self-report`, JSON.stringify(params))
            return await resp.json() as Absence
        }

        /**
         * Returns all staff with an approved sickness absence that covers today's date.
         * Used by the WC dashboard to cross-reference against the crewing board.
         */
        public async todaySick(): Promise<TodaySickResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/absences/today-sick`)
            return await resp.json() as TodaySickResponse
        }

        /**
         * FF can update dates/sick line on their own absence. WC can update anything.
         */
        public async updateAbsence(id: number, params: UpdateAbsenceDatesRequest): Promise<Absence> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/absences/${encodeURIComponent(id)}/update`, JSON.stringify(params))
            return await resp.json() as Absence
        }
    }
}

export namespace activity {
    export interface ActivityRecord {
        id: number
        type: ActivityType
        watch: string
        "financial_year": number
        quarter: number
        "item_number": number | null
        title: string
        address: string | null
        "engagement_date": string | null
        details: string | null
        completed: boolean
        "completed_at": string | null
        "sort_order": number
        "created_by": string
        "created_at": string
        "updated_at": string
    }

    export type ActivityType = "hfsv" | "hydrant" | "community"

    export interface CreateActivityRequest {
        type: ActivityType
        watch: string
        "financial_year": number
        quarter: number
        "item_number"?: number
        title?: string
        address?: string
        "engagement_date"?: string
        details?: string
    }

    export interface ListActivitiesRequest {
        type?: ActivityType
        watch?: string
        "financial_year"?: number
        quarter?: number
    }

    export interface ListActivitiesResponse {
        items: ActivityRecord[]
        "total_completed": number
        total: number
    }

    export interface SeedHfsvRequest {
        watch: string
        "financial_year": number
        quarter: number
    }

    export interface SeedHfsvResponse {
        created: number
        skipped: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.list = this.list.bind(this)
            this.remove = this.remove.bind(this)
            this.seedHfsv = this.seedHfsv.bind(this)
            this.toggle = this.toggle.bind(this)
            this.update = this.update.bind(this)
        }

        /**
         * POST /activities
         * Creates a single activity record (hydrant inspection or community engagement).
         * HFSV records should be bulk-seeded via POST /activities/seed-hfsv instead.
         */
        public async create(params: CreateActivityRequest): Promise<ActivityRecord> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/activities`, JSON.stringify(params))
            return await resp.json() as ActivityRecord
        }

        /**
         * GET /activities
         * Returns activity records filtered by type / watch / financial_year / quarter.
         */
        public async list(params: ListActivitiesRequest): Promise<ListActivitiesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "financial_year": params["financial_year"] === undefined ? undefined : String(params["financial_year"]),
                quarter:          params.quarter === undefined ? undefined : String(params.quarter),
                type:             params.type === undefined ? undefined : String(params.type),
                watch:            params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/activities`, undefined, {query})
            return await resp.json() as ListActivitiesResponse
        }

        /**
         * DELETE /activities/:id
         */
        public async remove(id: number): Promise<{
    /**
     * DELETE /activities/:id
     */
    ok: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/activities/${encodeURIComponent(id)}`)
            return await resp.json() as {
    /**
     * DELETE /activities/:id
     */
    ok: boolean
}
        }

        /**
         * POST /activities/seed-hfsv
         * Creates 36 numbered HFSV placeholders for a given watch/financial_year/quarter.
         * Safe to re-run: skips rows where item_number already exists for that slot.
         */
        public async seedHfsv(params: SeedHfsvRequest): Promise<SeedHfsvResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/activities/seed-hfsv`, JSON.stringify(params))
            return await resp.json() as SeedHfsvResponse
        }

        /**
         * PATCH /activities/:id/toggle
         * Flips the completed flag on an activity record.
         */
        public async toggle(id: number): Promise<ActivityRecord> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/activities/${encodeURIComponent(id)}/toggle`)
            return await resp.json() as ActivityRecord
        }

        /**
         * PATCH /activities/:id
         * Updates editable fields (title, address, engagement_date, details).
         */
        public async update(id: number, params: {
    title?: string
    address?: string
    "engagement_date"?: string
    details?: string
}): Promise<ActivityRecord> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/activities/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as ActivityRecord
        }
    }
}

export namespace admin {
    export interface ActivityLog {
        id: number
        "user_id": string
        action: string
        "entity_type": string
        "entity_id"?: string
        details?: any
        "ip_address"?: string
        "user_agent"?: string
        timestamp: string
    }

    export interface AssetReassignment {
        tasks: boolean
        inspections: boolean
        absences: boolean
    }

    export interface BootstrapRequest {
        setupToken: string
    }

    export interface BootstrapResponse {
        success: boolean
        message: string
        newRole: string
    }

    export interface ChangeRoleRequest {
        newRole: user.UserRole
    }

    export interface CheckWCResponse {
        hasWC: boolean
    }

    export interface CreateAccountRequest {
        name: string
        email: string
        password: string
        role: user.UserRole
        "watch_unit"?: string
        rank?: string
    }

    export interface CreateAccountResponse {
        success: boolean
        userId: string
        email: string
    }

    export interface CreateActivityLogRequest {
        "actor_user_id": string
        action: string
        "entity_type": string
        "entity_id"?: string
        metadata?: any
    }

    export interface DeactivateUserRequest {
        deactivationDate: string
        reason?: string
    }

    export interface GetActivityLogRequest {
        "user_id"?: string
        "entity_type"?: string
        limit?: number
        offset?: number
    }

    export interface GetActivityLogResponse {
        logs: ActivityLog[]
        total: number
    }

    export interface GetInviteLinkRequest {
        email: string
        "frontend_url"?: string
    }

    export interface GetInviteLinkResponse {
        "invite_link": string
        "user_name": string
        "user_email": string
        "already_active": boolean
    }

    export interface InviteUserRequest {
        email: string
        role: user.UserRole
        name?: string
    }

    export interface InviteUserResponse {
        success: boolean
        userId?: string
        invitationUrl?: string
    }

    export interface ReassignAssetsRequest {
        userId: string
        replacementUserId: string
        assets: AssetReassignment
    }

    export interface ReassignmentPreview {
        taskCount: number
        inspectionCount: number
        absenceCount: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.bootstrap = this.bootstrap.bind(this)
            this.changeRole = this.changeRole.bind(this)
            this.checkWC = this.checkWC.bind(this)
            this.createAccount = this.createAccount.bind(this)
            this.createActivityLog = this.createActivityLog.bind(this)
            this.deactivateUser = this.deactivateUser.bind(this)
            this.fixMyRole = this.fixMyRole.bind(this)
            this.getActivityLog = this.getActivityLog.bind(this)
            this.getInviteLink = this.getInviteLink.bind(this)
            this.getReassignmentPreview = this.getReassignmentPreview.bind(this)
            this.inviteUser = this.inviteUser.bind(this)
            this.listUsers = this.listUsers.bind(this)
            this.reactivateUser = this.reactivateUser.bind(this)
            this.reassignAssets = this.reassignAssets.bind(this)
        }

        public async bootstrap(params: BootstrapRequest): Promise<BootstrapResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                setupToken: params.setupToken,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/admin/bootstrap`, undefined, {query})
            return await resp.json() as BootstrapResponse
        }

        public async changeRole(userId: string, params: ChangeRoleRequest): Promise<void> {
            await this.baseClient.callTypedAPI("PUT", `/api/admin/users/${encodeURIComponent(userId)}/role`, JSON.stringify(params))
        }

        public async checkWC(): Promise<CheckWCResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/admin/check-wc`)
            return await resp.json() as CheckWCResponse
        }

        public async createAccount(params: CreateAccountRequest): Promise<CreateAccountResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/admin/create-account`, JSON.stringify(params))
            return await resp.json() as CreateAccountResponse
        }

        public async createActivityLog(params: CreateActivityLogRequest): Promise<ActivityLog> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/admin/activity-log`, JSON.stringify(params))
            return await resp.json() as ActivityLog
        }

        public async deactivateUser(userId: string, params: DeactivateUserRequest): Promise<void> {
            await this.baseClient.callTypedAPI("POST", `/api/admin/users/${encodeURIComponent(userId)}/deactivate`, JSON.stringify(params))
        }

        public async fixMyRole(): Promise<{
    success: boolean
    message: string
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/admin/fix-my-role`)
            return await resp.json() as {
    success: boolean
    message: string
}
        }

        public async getActivityLog(params: GetActivityLogRequest): Promise<GetActivityLogResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "entity_type": params["entity_type"],
                limit:         params.limit === undefined ? undefined : String(params.limit),
                offset:        params.offset === undefined ? undefined : String(params.offset),
                "user_id":     params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/admin/activity-log`, undefined, {query})
            return await resp.json() as GetActivityLogResponse
        }

        public async getInviteLink(params: GetInviteLinkRequest): Promise<GetInviteLinkResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/admin/get-invite-link`, JSON.stringify(params))
            return await resp.json() as GetInviteLinkResponse
        }

        public async getReassignmentPreview(userId: string): Promise<ReassignmentPreview> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/admin/reassign/${encodeURIComponent(userId)}/preview`)
            return await resp.json() as ReassignmentPreview
        }

        public async inviteUser(params: InviteUserRequest): Promise<InviteUserResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/admin/invite`, JSON.stringify(params))
            return await resp.json() as InviteUserResponse
        }

        public async listUsers(): Promise<{
    users: user.User[]
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/admin/users`)
            return await resp.json() as {
    users: user.User[]
}
        }

        public async reactivateUser(userId: string): Promise<void> {
            await this.baseClient.callTypedAPI("POST", `/api/admin/users/${encodeURIComponent(userId)}/reactivate`)
        }

        public async reassignAssets(params: ReassignAssetsRequest): Promise<void> {
            await this.baseClient.callTypedAPI("POST", `/api/admin/reassign`, JSON.stringify(params))
        }
    }
}

export namespace appliance {
    export interface Appliance {
        id: number
        "call_sign": string
        name: string
        type: ApplianceType
        "station_call_sign": string
        "station_name": string
        active: boolean
        "created_at": string
        "updated_at": string
    }

    export type ApplianceType = "Rescue" | "Decon" | "Aerial" | "Special" | "Other"

    export interface CheckDetailResponse {
        check: EquipmentCheck
        items: {
            id: number
            "check_id": number
            "equipment_item_id": number
            status: CheckItemStatus
            "quantity_checked": number
            notes: string | null
            "created_at": string
            "equipment_name": string
            "equipment_category": string
            "equipment_serial": string | null
        }[]
        "checked_by_name": string
    }

    export interface CheckItemInput {
        "equipment_item_id": number
        status: CheckItemStatus
        "quantity_checked": number
        notes?: string
    }

    export type CheckItemStatus = "OK" | "Defective" | "Missing"

    export type CheckStatus = "InProgress" | "Complete"

    export interface CreateApplianceRequest {
        "call_sign": string
        name: string
        type: ApplianceType
        "station_call_sign": string
        "station_name": string
    }

    export interface CreateEquipmentRequest {
        "appliance_id": number
        name: string
        category: EquipmentCategory
        "serial_number"?: string
        quantity?: number
    }

    export type DefectStatus = "Open" | "Ordered" | "Resolved"

    export type EquipmentCategory = "BA" | "Ladders" | "Hose" | "TIC" | "PPE" | "Tools" | "Medical" | "Other"

    export interface EquipmentCheck {
        id: number
        "appliance_id": number
        "checked_by": string
        watch: WatchType
        status: CheckStatus
        notes: string | null
        "started_at": string
        "completed_at": string | null
        "created_at": string
    }

    export interface EquipmentCheckItem {
        id: number
        "check_id": number
        "equipment_item_id": number
        status: CheckItemStatus
        "quantity_checked": number
        notes: string | null
        "created_at": string
    }

    export interface EquipmentDefect {
        id: number
        "check_item_id": number | null
        "equipment_item_id": number
        "appliance_id": number
        "reported_by": string
        description: string
        status: DefectStatus
        "reported_at": string
        "resolved_at": string | null
        "resolved_by": string | null
        "created_at": string
        "updated_at": string
    }

    export interface EquipmentItem {
        id: number
        "appliance_id": number
        name: string
        category: EquipmentCategory
        "serial_number": string | null
        quantity: number
        active: boolean
        "created_at": string
        "updated_at": string
    }

    export interface ListAppliancesRequest {
        "station_call_sign"?: string
        "active_only"?: boolean
    }

    export interface ListAppliancesResponse {
        appliances: Appliance[]
    }

    export interface ListChecksRequest {
        "appliance_id"?: number
        "checked_by"?: string
        watch?: string
        limit?: number
        offset?: number
    }

    export interface ListChecksResponse {
        checks: EquipmentCheck[]
        total: number
    }

    export interface ListDefectsRequest {
        "appliance_id"?: number
        status?: string
    }

    export interface ListDefectsResponse {
        defects: {
            id: number
            "check_item_id": number | null
            "equipment_item_id": number
            "appliance_id": number
            "reported_by": string
            description: string
            status: DefectStatus
            "reported_at": string
            "resolved_at": string | null
            "resolved_by": string | null
            "created_at": string
            "updated_at": string
            "equipment_name": string
            "appliance_call_sign": string
            "reported_by_name": string
            overdue: boolean
        }[]
    }

    export interface ListEquipmentRequest {
        "appliance_id": number
        "active_only"?: boolean
    }

    export interface ListEquipmentResponse {
        items: EquipmentItem[]
    }

    export interface StartCheckRequest {
        "appliance_id": number
        watch: WatchType
        items: CheckItemInput[]
        notes?: string
    }

    export interface StartCheckResponse {
        check: EquipmentCheck
        items: EquipmentCheckItem[]
        "defects_created": number
    }

    export interface UpdateApplianceRequest {
        "call_sign"?: string
        name?: string
        type?: ApplianceType
        active?: boolean
    }

    export interface UpdateDefectRequest {
        status?: DefectStatus
        description?: string
    }

    export interface UpdateEquipmentRequest {
        name?: string
        category?: EquipmentCategory
        "serial_number"?: string
        quantity?: number
        active?: boolean
    }

    export type WatchType = "Day" | "Night"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createAppliance = this.createAppliance.bind(this)
            this.createEquipment = this.createEquipment.bind(this)
            this.deleteEquipment = this.deleteEquipment.bind(this)
            this.getCheck = this.getCheck.bind(this)
            this.listAppliances = this.listAppliances.bind(this)
            this.listChecks = this.listChecks.bind(this)
            this.listDefects = this.listDefects.bind(this)
            this.listEquipment = this.listEquipment.bind(this)
            this.startCheck = this.startCheck.bind(this)
            this.updateAppliance = this.updateAppliance.bind(this)
            this.updateDefect = this.updateDefect.bind(this)
            this.updateEquipment = this.updateEquipment.bind(this)
        }

        public async createAppliance(params: CreateApplianceRequest): Promise<Appliance> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/appliances`, JSON.stringify(params))
            return await resp.json() as Appliance
        }

        public async createEquipment(params: CreateEquipmentRequest): Promise<EquipmentItem> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/appliances/equipment`, JSON.stringify(params))
            return await resp.json() as EquipmentItem
        }

        public async deleteEquipment(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/appliances/equipment/${encodeURIComponent(id)}`)
        }

        public async getCheck(id: number): Promise<CheckDetailResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/appliances/checks/${encodeURIComponent(id)}`)
            return await resp.json() as CheckDetailResponse
        }

        public async listAppliances(params: ListAppliancesRequest): Promise<ListAppliancesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "active_only":       params["active_only"] === undefined ? undefined : String(params["active_only"]),
                "station_call_sign": params["station_call_sign"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/appliances`, undefined, {query})
            return await resp.json() as ListAppliancesResponse
        }

        public async listChecks(params: ListChecksRequest): Promise<ListChecksResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "appliance_id": params["appliance_id"] === undefined ? undefined : String(params["appliance_id"]),
                "checked_by":   params["checked_by"],
                limit:          params.limit === undefined ? undefined : String(params.limit),
                offset:         params.offset === undefined ? undefined : String(params.offset),
                watch:          params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/appliances/checks`, undefined, {query})
            return await resp.json() as ListChecksResponse
        }

        public async listDefects(params: ListDefectsRequest): Promise<ListDefectsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "appliance_id": params["appliance_id"] === undefined ? undefined : String(params["appliance_id"]),
                status:         params.status,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/appliances/defects`, undefined, {query})
            return await resp.json() as ListDefectsResponse
        }

        public async listEquipment(params: ListEquipmentRequest): Promise<ListEquipmentResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "active_only":  params["active_only"] === undefined ? undefined : String(params["active_only"]),
                "appliance_id": String(params["appliance_id"]),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/appliances/equipment`, undefined, {query})
            return await resp.json() as ListEquipmentResponse
        }

        public async startCheck(params: StartCheckRequest): Promise<StartCheckResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/appliances/checks`, JSON.stringify(params))
            return await resp.json() as StartCheckResponse
        }

        public async updateAppliance(id: number, params: UpdateApplianceRequest): Promise<Appliance> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/appliances/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as Appliance
        }

        public async updateDefect(id: number, params: UpdateDefectRequest): Promise<EquipmentDefect> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/appliances/defects/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as EquipmentDefect
        }

        public async updateEquipment(id: number, params: UpdateEquipmentRequest): Promise<EquipmentItem> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/appliances/equipment/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as EquipmentItem
        }
    }
}

export namespace auth {
    export interface AuthParams {
        authorization?: string
    }
}

export namespace calendar {
    export interface CalendarEvent {
        id: number
        title: string
        description?: string
        "event_type": EventType
        "calendar_visibility": CalendarVisibility
        "start_time": string
        "end_time": string
        "all_day": boolean
        "user_id"?: string
        "is_watch_event": boolean
        location?: string
        attendees?: string[]
        color?: string
        watch?: string
        "source_type"?: string
        "source_id"?: number
        "created_by": string
        "created_at": string
        "updated_at": string
    }

    export type CalendarVisibility = "station" | "watch" | "personal"

    export interface CreateEventRequest {
        title: string
        description?: string
        "event_type": EventType
        "calendar_visibility"?: CalendarVisibility
        "start_time": string
        "end_time": string
        "all_day"?: boolean
        "user_id"?: string
        "is_watch_event"?: boolean
        location?: string
        attendees?: string[]
        color?: string
        watch?: string
        "source_type"?: string
        "source_id"?: number
        "created_by": string
    }

    export interface CreateInspectionEventRequest {
        "source_type": InspectionSourceType | null
        "source_id": number | null
        title: string
        location?: string
        "start_time": string
        "end_time": string
        "calendar_visibility": CalendarVisibility
        watch: string
        color?: string
        "assigned_by": string
        "assigned_to"?: string
        "due_date": string
    }

    export interface CreateInspectionEventResponse {
        "calendar_event_id": number
        "task_id": number
    }

    export interface DeleteEventRequest {
        "user_id": string
    }

    export type EventType = "watch" | "personal" | "training" | "meeting" | "inspection" | "maintenance" | "reminder"

    export interface ExportICSRequest {
        "user_id"?: string
        "start_date"?: string
        "end_date"?: string
    }

    export interface ExportICSResponse {
        content: string
        filename: string
    }

    export type InspectionSourceType = "hfsv" | "hydrant" | "multistory" | "operational" | "care_home"

    export interface ListEventsRequest {
        "user_id"?: string
        "is_watch_event"?: boolean
        "calendar_visibility"?: string
        "start_date"?: string
        "end_date"?: string
        limit?: number
        offset?: number
    }

    export interface ListEventsResponse {
        events: CalendarEvent[]
        total: number
    }

    export interface UpdateEventParams {
        "user_id": string
        updates: UpdateEventRequest
    }

    export interface UpdateEventRequest {
        title?: string
        description?: string
        "event_type"?: EventType
        "calendar_visibility"?: CalendarVisibility
        "start_time"?: string
        "end_time"?: string
        "all_day"?: boolean
        location?: string
        attendees?: string[]
        color?: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.createInspectionEvent = this.createInspectionEvent.bind(this)
            this.deleteEvent = this.deleteEvent.bind(this)
            this.exportICS = this.exportICS.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateEventRequest): Promise<CalendarEvent> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/calendar/events`, JSON.stringify(params))
            return await resp.json() as CalendarEvent
        }

        public async createInspectionEvent(params: CreateInspectionEventRequest): Promise<CreateInspectionEventResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/calendar/inspection-event`, JSON.stringify(params))
            return await resp.json() as CreateInspectionEventResponse
        }

        public async deleteEvent(id: number, params: DeleteEventRequest): Promise<void> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            await this.baseClient.callTypedAPI("DELETE", `/calendar/events/${encodeURIComponent(id)}`, undefined, {query})
        }

        public async exportICS(params: ExportICSRequest): Promise<ExportICSResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "end_date":   params["end_date"],
                "start_date": params["start_date"],
                "user_id":    params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/calendar/export.ics`, undefined, {query})
            return await resp.json() as ExportICSResponse
        }

        public async list(params: ListEventsRequest): Promise<ListEventsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "calendar_visibility": params["calendar_visibility"],
                "end_date":            params["end_date"],
                "is_watch_event":      params["is_watch_event"] === undefined ? undefined : String(params["is_watch_event"]),
                limit:                 params.limit === undefined ? undefined : String(params.limit),
                offset:                params.offset === undefined ? undefined : String(params.offset),
                "start_date":          params["start_date"],
                "user_id":             params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/calendar/events`, undefined, {query})
            return await resp.json() as ListEventsResponse
        }

        public async update(id: number, params: UpdateEventParams): Promise<CalendarEvent> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/calendar/events/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as CalendarEvent
        }
    }
}

export namespace crew {
    export interface BulkAssignResponse {
        tasks: task.Task[]
    }

    export interface BulkTaskAssignment {
        "assigned_to_ids": string[]
        title: string
        description?: string
        category: string
        priority?: string
        "stagger_days": number
        "start_date": string
    }

    export interface CrewStats {
        "total_firefighters": number
        "total_tasks": number
        "completed_tasks": number
        "overdue_tasks": number
        "upcoming_inspections": number
        "overdue_one_to_ones": number
        "completion_rate": number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.bulkAssign = this.bulkAssign.bind(this)
            this.getStats = this.getStats.bind(this)
        }

        public async bulkAssign(params: BulkTaskAssignment): Promise<BulkAssignResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/crew/bulk-assign`, JSON.stringify(params))
            return await resp.json() as BulkAssignResponse
        }

        public async getStats(): Promise<CrewStats> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/crew/stats`)
            return await resp.json() as CrewStats
        }
    }
}

export namespace crewing {
    export interface AddCrewingRequest {
        watch: string
        "shift_date": string
        "shift_type": ShiftType
        appliance: Appliance
        "user_id"?: string
        "external_name"?: string
        "crew_role": CrewRole
        "is_change_of_shift"?: boolean
        notes?: string
    }

    export type Appliance = "b10p1" | "b10p2" | "detached"

    export interface CopyPreviousRequest {
        watch: string
        "shift_date": string
        "shift_type": ShiftType
    }

    export type CrewRole = "oic" | "driver" | "ba" | "baeco" | "ff" | "detached"

    export interface CrewingEntry {
        id: number
        watch: string
        "shift_date": string
        "shift_type": ShiftType
        appliance: Appliance
        "user_id"?: string
        "user_name"?: string
        "user_system_role"?: string
        "user_rank"?: string
        "external_name"?: string
        "crew_role": CrewRole
        "is_change_of_shift": boolean
        notes?: string
        "created_at": string
    }

    export interface ListCrewingRequest {
        watch: string
        "shift_date": string
        "shift_type": ShiftType
    }

    export interface ListCrewingResponse {
        entries: CrewingEntry[]
    }

    export interface RosterMember {
        id: string
        name: string
        "system_role": string
        rank?: string
        "watch_unit"?: string
        "profile_watch"?: string
        ba: boolean
        prps: boolean
        "driver_lgv": boolean
        "driver_erd": boolean
    }

    export interface RosterRequest {
        watch: string
    }

    export interface RosterResponse {
        members: RosterMember[]
    }

    export type ShiftType = "1st Day" | "2nd Day" | "1st Night" | "2nd Night"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.add = this.add.bind(this)
            this.copyPrevious = this.copyPrevious.bind(this)
            this.list = this.list.bind(this)
            this.remove = this.remove.bind(this)
            this.roster = this.roster.bind(this)
        }

        /**
         * POST /crewing — add a crew member to an appliance for a shift
         */
        public async add(params: AddCrewingRequest): Promise<CrewingEntry> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/crewing`, JSON.stringify(params))
            return await resp.json() as CrewingEntry
        }

        /**
         * POST /crewing/copy-previous
         * Copies crewing from the most recent previous shift of the same type for this watch.
         * Change-of-shift (external) entries are NOT copied — they're specific to their shift.
         * Any existing crewing for the target date is replaced.
         */
        public async copyPrevious(params: CopyPreviousRequest): Promise<ListCrewingResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/crewing/copy-previous`, JSON.stringify(params))
            return await resp.json() as ListCrewingResponse
        }

        /**
         * GET /crewing — returns all crewing entries for a specific watch/date/shift
         */
        public async list(params: ListCrewingRequest): Promise<ListCrewingResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "shift_date": params["shift_date"],
                "shift_type": String(params["shift_type"]),
                watch:        params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/crewing`, undefined, {query})
            return await resp.json() as ListCrewingResponse
        }

        /**
         * DELETE /crewing/:id — remove a crew member from a shift
         */
        public async remove(id: number): Promise<{
    /**
     * DELETE /crewing/:id — remove a crew member from a shift
     */
    ok: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/crewing/${encodeURIComponent(id)}`)
            return await resp.json() as {
    /**
     * DELETE /crewing/:id — remove a crew member from a shift
     */
    ok: boolean
}
        }

        /**
         * GET /crewing/roster
         * Returns all active WC/CC/FF members for a watch, checking BOTH
         * users.watch_unit AND firefighter_profiles.watch so no one is missed
         * regardless of which column was populated for their account.
         */
        public async roster(params: RosterRequest): Promise<RosterResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                watch: params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/crewing/roster`, undefined, {query})
            return await resp.json() as RosterResponse
        }
    }
}

export namespace detachments {
    export interface CreateDetachmentRequest {
        "firefighter_id": string
        "firefighter_name": string
        "home_watch": string
        "to_station": string
        "detachment_date": string
        reason?: string
        notes?: string
    }

    export interface Detachment {
        id: number
        "firefighter_id": string
        "firefighter_name": string
        "home_watch": string
        "to_station": string
        "detachment_date": string
        reason?: string
        notes?: string
        "recorded_by_user_id": string
        "created_at": string
    }

    export interface GetRotaRequest {
        watch: string
    }

    export interface GetRotaResponse {
        members: RotaMember[]
    }

    export interface ListDetachmentsRequest {
        watch?: string
        limit?: number
        offset?: number
    }

    export interface ListDetachmentsResponse {
        detachments: Detachment[]
        total: number
    }

    export interface RotaMember {
        "firefighter_id": string
        "firefighter_name": string
        "watch_unit": string
        "last_detachment_date": string | null
        "last_to_station": string | null
        "last_reason": string | null
        "total_detachments": number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.getRota = this.getRota.bind(this)
            this.list = this.list.bind(this)
        }

        /**
         * POST /detachments — record a firefighter detachment event
         */
        public async create(params: CreateDetachmentRequest): Promise<Detachment> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/detachments`, JSON.stringify(params))
            return await resp.json() as Detachment
        }

        /**
         * GET /detachments/rota — fairness rota: all watch members sorted by last detachment (oldest first = next in line)
         */
        public async getRota(params: GetRotaRequest): Promise<GetRotaResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                watch: params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/detachments/rota`, undefined, {query})
            return await resp.json() as GetRotaResponse
        }

        /**
         * GET /detachments — list detachment history, optionally filtered by watch
         */
        public async list(params: ListDetachmentsRequest): Promise<ListDetachmentsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
                watch:  params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/detachments`, undefined, {query})
            return await resp.json() as ListDetachmentsResponse
        }
    }
}

export namespace dictionary {
    export interface CreateDictionaryRequest {
        type: DictionaryType
        value: string
        active?: boolean
    }

    export interface Dictionary {
        id: number
        type: DictionaryType
        value: string
        active: boolean
        "created_at": string
    }

    export type DictionaryType = "skill" | "cert"

    export interface ListDictionaryRequest {
        type?: DictionaryType
        active?: boolean
    }

    export interface ListDictionaryResponse {
        items: Dictionary[]
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteDictionary = this.deleteDictionary.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateDictionaryRequest): Promise<Dictionary> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/dictionary`, JSON.stringify(params))
            return await resp.json() as Dictionary
        }

        public async deleteDictionary(id: number): Promise<{
    success: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/dictionary/${encodeURIComponent(id)}`)
            return await resp.json() as {
    success: boolean
}
        }

        public async list(params: ListDictionaryRequest): Promise<ListDictionaryResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                active: params.active === undefined ? undefined : String(params.active),
                type:   params.type === undefined ? undefined : String(params.type),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/dictionary`, undefined, {query})
            return await resp.json() as ListDictionaryResponse
        }

        public async update(id: number, params: {
    value?: string
    active?: boolean
}): Promise<Dictionary> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/dictionary/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as Dictionary
        }
    }
}

export namespace frontend {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.assets = this.assets.bind(this)
        }

        public async assets(path: string[]): Promise<void> {
            await this.baseClient.callTypedAPI("HEAD", `/frontend/${path.map(encodeURIComponent).join("/")}`)
        }
    }
}

export namespace h4h {
    export interface H4HEntry {
        id: number
        "creditor_user_id": string
        "creditor_name": string
        "debtor_user_id": string
        "debtor_name": string
        "shift_date": string
        "shift_adjustment_id": number | null
        "payback_shift_adjustment_id": number | null
        status: "pending" | "settled"
        "settled_at": string | null
        "settled_by_user_id": string | null
        "settled_via": "auto" | "manual" | null
        notes: string | null
        "created_at": string
    }

    export interface ListH4HRequest {
        "user_id": string
        status?: "pending" | "settled"
    }

    export interface ListH4HResponse {
        "owed_to_me": H4HEntry[]
        "i_owe": H4HEntry[]
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.list = this.list.bind(this)
            this.settle = this.settle.bind(this)
        }

        public async list(params: ListH4HRequest): Promise<ListH4HResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                status:    params.status === undefined ? undefined : String(params.status),
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/h4h`, undefined, {query})
            return await resp.json() as ListH4HResponse
        }

        public async settle(id: number): Promise<H4HEntry> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/h4h/${encodeURIComponent(id)}/settle`)
            return await resp.json() as H4HEntry
        }
    }
}

export namespace handover {
    export interface CreateHandoverRequest {
        watch: string
        "shift_type": ShiftType
        "shift_date": string
        incidents?: string
        "outstanding_tasks"?: string
        "equipment_notes"?: string
        "staff_notes"?: string
        "general_notes"?: string
    }

    export interface DeleteHandoverResponse {
        success: boolean
    }

    export interface GetLatestResponse {
        handover: Handover | null
        found: boolean
    }

    export interface Handover {
        id: number
        watch: string
        "shift_type": ShiftType
        "shift_date": string
        "written_by_user_id": string
        "written_by_name"?: string
        incidents?: string
        "outstanding_tasks"?: string
        "equipment_notes"?: string
        "staff_notes"?: string
        "general_notes": string
        "created_at": string
        "updated_at": string
    }

    export interface ListHandoversResponse {
        handovers: Handover[]
        total: number
    }

    export interface ListRequest {
        watch?: string
        limit?: number
        offset?: number
    }

    export type ShiftType = "Day" | "Night"

    export interface UpdateHandoverRequest {
        incidents?: string
        "outstanding_tasks"?: string
        "equipment_notes"?: string
        "staff_notes"?: string
        "general_notes"?: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteHandover = this.deleteHandover.bind(this)
            this.getLatest = this.getLatest.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateHandoverRequest): Promise<Handover> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/handovers`, JSON.stringify(params))
            return await resp.json() as Handover
        }

        public async deleteHandover(id: number): Promise<DeleteHandoverResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/handovers/${encodeURIComponent(id)}`)
            return await resp.json() as DeleteHandoverResponse
        }

        public async getLatest(params: {
    watch?: string
}): Promise<GetLatestResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                watch: params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/handovers/latest`, undefined, {query})
            return await resp.json() as GetLatestResponse
        }

        public async list(params: ListRequest): Promise<ListHandoversResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
                watch:  params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/handovers`, undefined, {query})
            return await resp.json() as ListHandoversResponse
        }

        public async update(id: number, params: UpdateHandoverRequest): Promise<Handover> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/handovers/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as Handover
        }
    }
}

export namespace inspection {
    export interface CreateInspectionParams {
        type: InspectionType
        address: string
        priority?: InspectionPriority
        "scheduled_for": string
        "assigned_crew_ids"?: string[]
        status?: InspectionStatus
        notes?: string
    }

    export interface Inspection {
        id: number
        type: InspectionType
        address: string
        priority: InspectionPriority
        "scheduled_for": string
        "assigned_crew_ids"?: string[]
        status: InspectionStatus
        notes?: string
        "completed_date"?: string
        "created_at": string
        "updated_at": string
    }

    export type InspectionPriority = "Low" | "Med" | "High"

    export type InspectionStatus = "Planned" | "InProgress" | "Complete"

    export type InspectionType = "HighRise" | "LocalProperty" | "Hydrant" | "Other"

    export interface ListInspectionsRequest {
        status?: InspectionStatus
        "inspector_id"?: string
        "inspection_type"?: string
        limit?: number
        offset?: number
    }

    export interface ListInspectionsResponse {
        inspections: Inspection[]
        total: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateInspectionParams): Promise<Inspection> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/inspections`, JSON.stringify(params))
            return await resp.json() as Inspection
        }

        public async list(params: ListInspectionsRequest): Promise<ListInspectionsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "inspection_type": params["inspection_type"],
                "inspector_id":    params["inspector_id"],
                limit:             params.limit === undefined ? undefined : String(params.limit),
                offset:            params.offset === undefined ? undefined : String(params.offset),
                status:            params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/inspections`, undefined, {query})
            return await resp.json() as ListInspectionsResponse
        }

        public async update(id: number, params: {
    type?: InspectionType
    address?: string
    priority?: InspectionPriority
    "scheduled_for"?: string
    "assigned_crew_ids"?: string[]
    status?: InspectionStatus
    notes?: string
    "completed_date"?: string
}): Promise<Inspection> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/inspections/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as Inspection
        }
    }
}

export namespace inspection_plans {
    export interface CareHomeValidation {
        id: number
        address: string
        position: number
        "created_at": string
        "updated_at": string
    }

    export interface CreateCareHomeRequest {
        address: string
    }

    export interface CreateHydrantRequest {
        "area_code": string
        street: string
        section: string
        year: number
        watch?: WatchName
    }

    export interface CreateMultistoryRequest {
        address: string
        "q1_watch"?: WatchName
        "q2_watch"?: WatchName
        "q3_watch"?: WatchName
        "q4_watch"?: WatchName
    }

    export interface CreateOperationalRequest {
        address: string
        uprn: string
        watch?: WatchName
    }

    export interface GenerateAssignmentsRequest {
        year: number
        quarter?: number
    }

    export interface GenerateAssignmentsResponse {
        created: number
        skipped: number
    }

    export interface HydrantRegister {
        id: number
        "area_code": string
        street: string
        section: string
        year: number
        watch?: WatchName
        position: number
        "created_at": string
        "updated_at": string
    }

    export interface InspectionAssignment {
        id: number
        "plan_type": "multistory" | "care_home" | "hydrant" | "operational"
        "plan_id": number
        label: string
        watch: WatchName
        year: number
        quarter: number | null
        status: "pending" | "complete"
        "completed_at": string | null
        "completed_by": number | null
        notes: string | null
        "created_at": string
        "updated_at": string
    }

    export interface ListAssignmentsRequest {
        watch?: string
        year?: number
        quarter?: number
        "plan_type"?: string
        status?: string
    }

    export interface ListAssignmentsResponse {
        items: InspectionAssignment[]
        totals: {
            pending: number
            complete: number
        }
    }

    export interface ListCareHomeResponse {
        items: CareHomeValidation[]
    }

    export interface ListHydrantResponse {
        items: HydrantRegister[]
    }

    export interface ListMultistoryResponse {
        items: MultistoryInspection[]
    }

    export interface ListOperationalResponse {
        items: OperationalInspection[]
    }

    export interface MultistoryInspection {
        id: number
        address: string
        "q1_watch"?: WatchName
        "q2_watch"?: WatchName
        "q3_watch"?: WatchName
        "q4_watch"?: WatchName
        position: number
        "created_at": string
        "updated_at": string
    }

    export interface OperationalInspection {
        id: number
        address: string
        uprn: string
        watch?: WatchName
        position: number
        "created_at": string
        "updated_at": string
    }

    export type WatchName = "Red" | "White" | "Green" | "Blue" | "Amber"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createCareHome = this.createCareHome.bind(this)
            this.createHydrant = this.createHydrant.bind(this)
            this.createMultistory = this.createMultistory.bind(this)
            this.createOperational = this.createOperational.bind(this)
            this.deleteCareHome = this.deleteCareHome.bind(this)
            this.deleteHydrant = this.deleteHydrant.bind(this)
            this.deleteMultistory = this.deleteMultistory.bind(this)
            this.deleteOperational = this.deleteOperational.bind(this)
            this.generateAssignments = this.generateAssignments.bind(this)
            this.listAssignments = this.listAssignments.bind(this)
            this.listCareHomes = this.listCareHomes.bind(this)
            this.listHydrants = this.listHydrants.bind(this)
            this.listMultistory = this.listMultistory.bind(this)
            this.listOperational = this.listOperational.bind(this)
            this.updateAssignment = this.updateAssignment.bind(this)
            this.updateCareHome = this.updateCareHome.bind(this)
            this.updateHydrant = this.updateHydrant.bind(this)
            this.updateMultistory = this.updateMultistory.bind(this)
            this.updateOperational = this.updateOperational.bind(this)
        }

        public async createCareHome(params: CreateCareHomeRequest): Promise<CareHomeValidation> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/inspection-plans/care-homes`, JSON.stringify(params))
            return await resp.json() as CareHomeValidation
        }

        public async createHydrant(params: CreateHydrantRequest): Promise<HydrantRegister> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/inspection-plans/hydrants`, JSON.stringify(params))
            return await resp.json() as HydrantRegister
        }

        public async createMultistory(params: CreateMultistoryRequest): Promise<MultistoryInspection> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/inspection-plans/multistory`, JSON.stringify(params))
            return await resp.json() as MultistoryInspection
        }

        public async createOperational(params: CreateOperationalRequest): Promise<OperationalInspection> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/inspection-plans/operational`, JSON.stringify(params))
            return await resp.json() as OperationalInspection
        }

        public async deleteCareHome(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/inspection-plans/care-homes/${encodeURIComponent(id)}`)
        }

        public async deleteHydrant(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/inspection-plans/hydrants/${encodeURIComponent(id)}`)
        }

        public async deleteMultistory(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/inspection-plans/multistory/${encodeURIComponent(id)}`)
        }

        public async deleteOperational(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/inspection-plans/operational/${encodeURIComponent(id)}`)
        }

        /**
         * POST /inspection-plans/assignments/generate
         * WC/CC only — reads the 4 plan tables and creates assignments for the given year.
         * Passing quarter= limits multi-story to that quarter only; omitting it generates all quarters.
         * Care homes always generate annual assignments (quarter = null) for ALL watches.
         * Hydrants + OIs generate annual assignments for their own watch field.
         * Uses ON CONFLICT DO NOTHING so it is safe to re-run.
         */
        public async generateAssignments(params: GenerateAssignmentsRequest): Promise<GenerateAssignmentsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/inspection-plans/assignments/generate`, JSON.stringify(params))
            return await resp.json() as GenerateAssignmentsResponse
        }

        /**
         * GET /inspection-plans/assignments
         * Returns assignments filtered by optional watch, year, quarter, plan_type, status.
         */
        public async listAssignments(params: ListAssignmentsRequest): Promise<ListAssignmentsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "plan_type": params["plan_type"],
                quarter:     params.quarter === undefined ? undefined : String(params.quarter),
                status:      params.status,
                watch:       params.watch,
                year:        params.year === undefined ? undefined : String(params.year),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/inspection-plans/assignments`, undefined, {query})
            return await resp.json() as ListAssignmentsResponse
        }

        public async listCareHomes(): Promise<ListCareHomeResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/inspection-plans/care-homes`)
            return await resp.json() as ListCareHomeResponse
        }

        public async listHydrants(): Promise<ListHydrantResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/inspection-plans/hydrants`)
            return await resp.json() as ListHydrantResponse
        }

        public async listMultistory(): Promise<ListMultistoryResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/inspection-plans/multistory`)
            return await resp.json() as ListMultistoryResponse
        }

        public async listOperational(): Promise<ListOperationalResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/inspection-plans/operational`)
            return await resp.json() as ListOperationalResponse
        }

        /**
         * PATCH /inspection-plans/assignments/:id
         * WC/CC only — marks an assignment complete or reverts it to pending.
         * Side-effect: if the assignment is multistory, recalculates the HighRise target
         * actual_count for the assignment's year so the Targets dashboard stays in sync.
         */
        public async updateAssignment(id: number, params: {
    status: "pending" | "complete"
    notes?: string
}): Promise<InspectionAssignment> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/inspection-plans/assignments/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as InspectionAssignment
        }

        public async updateCareHome(id: number, params: {
    address?: string
    position?: number
}): Promise<CareHomeValidation> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/inspection-plans/care-homes/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as CareHomeValidation
        }

        public async updateHydrant(id: number, params: {
    "area_code"?: string
    street?: string
    section?: string
    year?: number
    watch?: WatchName | null
    position?: number
}): Promise<HydrantRegister> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/inspection-plans/hydrants/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as HydrantRegister
        }

        public async updateMultistory(id: number, params: {
    address?: string
    "q1_watch"?: WatchName | null
    "q2_watch"?: WatchName | null
    "q3_watch"?: WatchName | null
    "q4_watch"?: WatchName | null
    position?: number
}): Promise<MultistoryInspection> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/inspection-plans/multistory/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as MultistoryInspection
        }

        public async updateOperational(id: number, params: {
    address?: string
    uprn?: string
    watch?: WatchName | null
    position?: number
}): Promise<OperationalInspection> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/inspection-plans/operational/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as OperationalInspection
        }
    }
}

export namespace localauth {
    export interface AuthResponse {
        user: {
            id: string
            email: string
            name: string
            role: string
        }
        token: string
    }

    export interface ChangePasswordRequest {
        "current_password": string
        "new_password": string
    }

    export interface ChangePasswordResponse {
        success: boolean
    }

    export interface CreateAdminResponse {
        success: boolean
        message: string
    }

    export interface ForgotPasswordRequest {
        email: string
    }

    export interface ForgotPasswordResponse {
        success: boolean
        "email_sent": boolean
    }

    export interface ResetPasswordRequest {
        token: string
        "new_password": string
    }

    export interface ResetPasswordResponse {
        success: boolean
    }

    export interface ResetUserPasswordRequest {
        userId: string
        "new_password": string
    }

    export interface ResetUserPasswordResponse {
        success: boolean
    }

    export interface SignInRequest {
        email: string
        password: string
    }

    export interface SignUpRequest {
        email: string
        password: string
        name: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.changePassword = this.changePassword.bind(this)
            this.createAdmin = this.createAdmin.bind(this)
            this.forgotPassword = this.forgotPassword.bind(this)
            this.resetPassword = this.resetPassword.bind(this)
            this.resetUserPassword = this.resetUserPassword.bind(this)
            this.signIn = this.signIn.bind(this)
            this.signOut = this.signOut.bind(this)
            this.signUp = this.signUp.bind(this)
        }

        public async changePassword(params: ChangePasswordRequest): Promise<ChangePasswordResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/change-password`, JSON.stringify(params))
            return await resp.json() as ChangePasswordResponse
        }

        public async createAdmin(): Promise<CreateAdminResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/localauth/create-admin`)
            return await resp.json() as CreateAdminResponse
        }

        public async forgotPassword(params: ForgotPasswordRequest): Promise<ForgotPasswordResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/forgot-password`, JSON.stringify(params))
            return await resp.json() as ForgotPasswordResponse
        }

        public async resetPassword(params: ResetPasswordRequest): Promise<ResetPasswordResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/reset-password`, JSON.stringify(params))
            return await resp.json() as ResetPasswordResponse
        }

        public async resetUserPassword(params: ResetUserPasswordRequest): Promise<ResetUserPasswordResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/reset-user-password`, JSON.stringify(params))
            return await resp.json() as ResetUserPasswordResponse
        }

        public async signIn(params: SignInRequest): Promise<AuthResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/signin`, JSON.stringify(params))
            return await resp.json() as AuthResponse
        }

        public async signOut(): Promise<{
    success: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/signout`)
            return await resp.json() as {
    success: boolean
}
        }

        public async signUp(params: SignUpRequest): Promise<AuthResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth/signup`, JSON.stringify(params))
            return await resp.json() as AuthResponse
        }
    }
}

export namespace mock {
    export interface AbsenceTriggers {
        id: string
        sixMonth: {
            episodes: number
            days: number
        }
        oneYear: {
            episodes: number
            days: number
        }
    }

    export interface DictionarySkill {
        id: string
        type: "skill"
        value: string
        active: boolean
        createdAt: string
    }

    export interface GetMockInspectionsRequest {
        status?: string
        type?: string
    }

    export interface GetMockInspectionsResponse {
        inspections: inspection.Inspection[]
        total: number
    }

    export interface GetMockPolicyDocsRequest {
        category?: string
    }

    export interface GetMockPolicyDocsResponse {
        docs: policy.PolicyDoc[]
        total: number
    }

    export interface GetMockPolicyQueriesRequest {
        "user_id"?: string
    }

    export interface GetMockPolicyQueriesResponse {
        queries: policy.PolicyQuery[]
        total: number
    }

    export interface GetMockTargetsRequest {
        metric?: string
        status?: string
    }

    export interface GetMockTargetsResponse {
        targets: targets.Target[]
        total: number
    }

    export interface GetMockTasksRequest {
        status?: string
        "assigned_to"?: string
    }

    export interface GetMockTasksResponse {
        tasks: task.Task[]
        total: number
    }

    export interface GetMockUsersRequest {
        role?: string
    }

    export interface GetMockUsersResponse {
        users: user.User[]
        total: number
    }

    export interface MockAbsence {
        id: string
        firefighterId: string
        type: "sickness" | "AL" | "TOIL" | "parental" | "other"
        startDate: string
        endDate: string
        totalDays: number
        reason?: string
        createdByUserId: string
        createdAt: string
    }

    export interface MockAskPolicyRequest {
        question: string
    }

    export interface MockAskPolicyResponse {
        answer: string
        citations: {
            "doc_title": string
            page: number
        }[]
        confidence: number
    }

    export interface MockPerson {
        id: string
        email: string
        name: string
        role: "WC" | "CC" | "FF" | "RO"
        watch?: "Green" | "Red" | "White" | "Blue" | "Amber"
        phone?: string
        staffNumber?: string
        niNumber?: string
        rank: "SC" | "WC" | "CC" | "FF"
        skills: string[]
        driverPathway: {
            status: "medical_due" | "application_sent" | "awaiting_theory" | "awaiting_course" | "passed_LGV" | "awaiting_ERD" | "passed"
            lgvPassedDate?: string
        }
        notes: string
        lastConversation?: {
            date: string
            text: string
        }
        customFields: { [key: string]: string | number | boolean | null }
        isActive: boolean
        createdAt: string
        updatedAt: string
    }

    export interface UpdateAbsenceTriggersRequest {
        sixMonth?: {
            episodes?: number
            days?: number
        }
        oneYear?: {
            episodes?: number
            days?: number
        }
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.createMockAbsence = this.createMockAbsence.bind(this)
            this.createSkill = this.createSkill.bind(this)
            this.getAbsenceTriggers = this.getAbsenceTriggers.bind(this)
            this.getMockAbsences = this.getMockAbsences.bind(this)
            this.getMockInspection = this.getMockInspection.bind(this)
            this.getMockInspections = this.getMockInspections.bind(this)
            this.getMockPeople = this.getMockPeople.bind(this)
            this.getMockPerson = this.getMockPerson.bind(this)
            this.getMockPolicyDocs = this.getMockPolicyDocs.bind(this)
            this.getMockPolicyQueries = this.getMockPolicyQueries.bind(this)
            this.getMockTarget = this.getMockTarget.bind(this)
            this.getMockTargets = this.getMockTargets.bind(this)
            this.getMockTask = this.getMockTask.bind(this)
            this.getMockTasks = this.getMockTasks.bind(this)
            this.getMockUser = this.getMockUser.bind(this)
            this.getMockUsers = this.getMockUsers.bind(this)
            this.getSkills = this.getSkills.bind(this)
            this.mockAskPolicy = this.mockAskPolicy.bind(this)
            this.updateAbsenceTriggers = this.updateAbsenceTriggers.bind(this)
            this.updateMockPerson = this.updateMockPerson.bind(this)
        }

        public async createMockAbsence(id: string, params: {
    type: "sickness" | "AL" | "TOIL" | "parental" | "other"
    startDate: string
    endDate: string
    totalDays: number
    reason?: string
    createdByUserId: string
}): Promise<{
    data: MockAbsence
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/people/${encodeURIComponent(id)}/absences`, JSON.stringify(params))
            return await resp.json() as {
    data: MockAbsence
}
        }

        public async createSkill(params: {
    value: string
    active?: boolean
}): Promise<{
    data: DictionarySkill
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/dictionaries/skills`, JSON.stringify(params))
            return await resp.json() as {
    data: DictionarySkill
}
        }

        public async getAbsenceTriggers(): Promise<{
    data: AbsenceTriggers
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/settings/absence-triggers`)
            return await resp.json() as {
    data: AbsenceTriggers
}
        }

        public async getMockAbsences(id: string): Promise<{
    data: MockAbsence[]
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/people/${encodeURIComponent(id)}/absences`)
            return await resp.json() as {
    data: MockAbsence[]
}
        }

        public async getMockInspection(id: number): Promise<inspection.Inspection> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/inspections/${encodeURIComponent(id)}`)
            return await resp.json() as inspection.Inspection
        }

        public async getMockInspections(params: GetMockInspectionsRequest): Promise<GetMockInspectionsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                status: params.status,
                type:   params.type,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/inspections`, undefined, {query})
            return await resp.json() as GetMockInspectionsResponse
        }

        public async getMockPeople(): Promise<{
    data: MockPerson[]
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/people`)
            return await resp.json() as {
    data: MockPerson[]
}
        }

        public async getMockPerson(id: string): Promise<{
    data: MockPerson | null
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/people/${encodeURIComponent(id)}`)
            return await resp.json() as {
    data: MockPerson | null
}
        }

        public async getMockPolicyDocs(params: GetMockPolicyDocsRequest): Promise<GetMockPolicyDocsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category: params.category,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/policies/docs`, undefined, {query})
            return await resp.json() as GetMockPolicyDocsResponse
        }

        public async getMockPolicyQueries(params: GetMockPolicyQueriesRequest): Promise<GetMockPolicyQueriesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/policies/queries`, undefined, {query})
            return await resp.json() as GetMockPolicyQueriesResponse
        }

        public async getMockTarget(id: number): Promise<targets.Target> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/targets/${encodeURIComponent(id)}`)
            return await resp.json() as targets.Target
        }

        public async getMockTargets(params: GetMockTargetsRequest): Promise<GetMockTargetsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                metric: params.metric,
                status: params.status,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/targets`, undefined, {query})
            return await resp.json() as GetMockTargetsResponse
        }

        public async getMockTask(id: number): Promise<task.Task> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/tasks/${encodeURIComponent(id)}`)
            return await resp.json() as task.Task
        }

        public async getMockTasks(params: GetMockTasksRequest): Promise<GetMockTasksResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "assigned_to": params["assigned_to"],
                status:        params.status,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/tasks`, undefined, {query})
            return await resp.json() as GetMockTasksResponse
        }

        public async getMockUser(id: string): Promise<user.User> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/users/${encodeURIComponent(id)}`)
            return await resp.json() as user.User
        }

        public async getMockUsers(params: GetMockUsersRequest): Promise<GetMockUsersResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                role: params.role,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/mock/users`, undefined, {query})
            return await resp.json() as GetMockUsersResponse
        }

        public async getSkills(): Promise<{
    data: DictionarySkill[]
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/dictionaries/skills`)
            return await resp.json() as {
    data: DictionarySkill[]
}
        }

        public async mockAskPolicy(params: MockAskPolicyRequest): Promise<MockAskPolicyResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/mock/policies/ask`, JSON.stringify(params))
            return await resp.json() as MockAskPolicyResponse
        }

        public async updateAbsenceTriggers(params: UpdateAbsenceTriggersRequest): Promise<{
    data: AbsenceTriggers
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/settings/absence-triggers`, JSON.stringify(params))
            return await resp.json() as {
    data: AbsenceTriggers
}
        }

        public async updateMockPerson(id: string, params: {
    email?: string
    name?: string
    role?: "WC" | "CC" | "FF" | "RO"
    watch?: "Green" | "Red" | "White" | "Blue" | "Amber"
    phone?: string
    staffNumber?: string
    niNumber?: string
    rank?: "SC" | "WC" | "CC" | "FF"
    skills?: string[]
    driverPathway?: {
        status: "medical_due" | "application_sent" | "awaiting_theory" | "awaiting_course" | "passed_LGV" | "awaiting_ERD" | "passed"
        lgvPassedDate?: string
    }
    notes?: string
    lastConversation?: {
        date: string
        text: string
    }
    customFields?: { [key: string]: string | number | boolean | null }
    isActive?: boolean
    createdAt?: string
    updatedAt?: string
}): Promise<{
    data: MockPerson
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/people/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as {
    data: MockPerson
}
        }
    }
}

export namespace note {
    export interface CreateNoteRequest {
        "profile_id": number
        "note_text": string
        "next_follow_up_date"?: string
        "reminder_enabled"?: boolean
        "reminder_recipient_user_id"?: string
    }

    export interface DeleteNoteResponse {
        success: boolean
    }

    export interface ListNotesRequest {
        "profile_id"?: number
        "user_id"?: string
    }

    export interface ListNotesResponse {
        notes: Note[]
    }

    export interface Note {
        id: number
        "profile_id": number
        "created_by_user_id": string
        "note_text": string
        "next_follow_up_date"?: string
        "reminder_enabled": boolean
        "reminder_recipient_user_id"?: string
        "calendar_event_id"?: number
        attachments?: NoteAttachment[]
        "created_at": string
        "updated_at": string
    }

    export interface NoteAttachment {
        filename: string
        url: string
        fileType: string
        fileSize: number
        uploadedAt: string
    }

    export interface SaveAttachmentRequest {
        "file_key": string
        filename: string
        "file_type": string
        "file_size": number
    }

    export interface UploadNoteAttachmentRequest {
        filename: string
        "content_type": string
    }

    export interface UploadNoteAttachmentResponse {
        "upload_url": string
        "file_key": string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteNote = this.deleteNote.bind(this)
            this.getUploadUrl = this.getUploadUrl.bind(this)
            this.list = this.list.bind(this)
            this.saveAttachment = this.saveAttachment.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateNoteRequest): Promise<Note> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notes`, JSON.stringify(params))
            return await resp.json() as Note
        }

        public async deleteNote(id: number): Promise<DeleteNoteResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/notes/${encodeURIComponent(id)}`)
            return await resp.json() as DeleteNoteResponse
        }

        public async getUploadUrl(note_id: number, params: UploadNoteAttachmentRequest): Promise<UploadNoteAttachmentResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notes/${encodeURIComponent(note_id)}/attachments/upload-url`, JSON.stringify(params))
            return await resp.json() as UploadNoteAttachmentResponse
        }

        public async list(params: ListNotesRequest): Promise<ListNotesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "profile_id": params["profile_id"] === undefined ? undefined : String(params["profile_id"]),
                "user_id":    params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notes`, undefined, {query})
            return await resp.json() as ListNotesResponse
        }

        public async saveAttachment(note_id: number, params: SaveAttachmentRequest): Promise<{
    success: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notes/${encodeURIComponent(note_id)}/attachments`, JSON.stringify(params))
            return await resp.json() as {
    success: boolean
}
        }

        public async update(id: number, params: {
    "note_text"?: string
    "next_follow_up_date"?: string
    "reminder_enabled"?: boolean
    "reminder_recipient_user_id"?: string
}): Promise<Note> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/notes/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as Note
        }
    }
}

export namespace notification {
    export interface ListNotificationsResponse {
        notifications: Notification[]
        "unread_count": number
    }

    export interface Notification {
        id: number
        "user_id": string
        type: NotificationType
        title: string
        message: string
        "entity_type"?: string
        "entity_id"?: string
        link?: string
        read: boolean
        "created_at": string
    }

    export type NotificationType = "sick_booking" | "cert_expiry" | "task_overdue" | "crewing_gap" | "general"

    export interface RefreshResponse {
        generated: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.list = this.list.bind(this)
            this.markAllRead = this.markAllRead.bind(this)
            this.markRead = this.markRead.bind(this)
            this.refresh = this.refresh.bind(this)
        }

        public async list(): Promise<ListNotificationsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/notifications`)
            return await resp.json() as ListNotificationsResponse
        }

        public async markAllRead(): Promise<{
    success: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/read-all`)
            return await resp.json() as {
    success: boolean
}
        }

        public async markRead(id: number): Promise<{
    success: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/${encodeURIComponent(id)}/read`)
            return await resp.json() as {
    success: boolean
}
        }

        /**
         * Refreshes notifications for the current user.
         * Generates cert_expiry, task_overdue, and crewing_gap notifications.
         * Deduplicates: will not create a duplicate notification for the same entity
         * if one already exists that is unread.
         */
        public async refresh(): Promise<RefreshResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/notifications/refresh`)
            return await resp.json() as RefreshResponse
        }
    }
}

export namespace policy {
    export interface AskPolicyRequest {
        query: string
    }

    export interface AskPolicyResponse {
        answer: string
        citations: Citation[]
        confidence?: number
    }

    export interface Citation {
        "doc_title": string
        page: number
    }

    export interface CreatePolicyDocRequest {
        title: string
        "file_name": string
        "file_path": string
        "file_size": number
        category?: string
        tags?: string[]
        version?: string
        "effective_date"?: string
        "uploaded_by": string
        "total_pages"?: number
    }

    export interface GetDownloadUrlResponse {
        "download_url": string
    }

    export interface GetUploadUrlRequest {
        "file_name": string
    }

    export interface GetUploadUrlResponse {
        "upload_url": string
        "file_path": string
    }

    export interface ListPoliciesRequest {
        category?: string
        limit?: number
        offset?: number
    }

    export interface ListPoliciesResponse {
        policies: PolicyDoc[]
        total: number
    }

    export interface PolicyDoc {
        id: number
        title: string
        category?: string
        version?: string
        "file_url"?: string
        "vector_id"?: string
        "uploaded_at": string
        "review_date"?: string
        "file_name": string
        "file_path": string
        "file_size": number
        tags?: string[]
        "effective_date"?: string
        "uploaded_by": string
        "total_pages"?: number
        "created_at": string
        "updated_at": string
    }

    export interface PolicyQuery {
        id: number
        "asked_by_user_id": string
        question: string
        answer: string
        citations: Citation[]
        confidence?: number
        "created_at": string
    }

    export interface ProcessEmbeddingResponse {
        "policy_id": number
        "vector_id": string
        success: boolean
    }

    export interface QueryHistoryRequest {
        "user_id"?: string
        limit?: number
        offset?: number
    }

    export interface QueryHistoryResponse {
        queries: PolicyQuery[]
        total: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.ask = this.ask.bind(this)
            this.getDownloadUrl = this.getDownloadUrl.bind(this)
            this.getUploadUrl = this.getUploadUrl.bind(this)
            this.list = this.list.bind(this)
            this.processEmbedding = this.processEmbedding.bind(this)
            this.queryHistory = this.queryHistory.bind(this)
            this.upload = this.upload.bind(this)
        }

        public async ask(params: AskPolicyRequest): Promise<AskPolicyResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/policies/ask`, JSON.stringify(params))
            return await resp.json() as AskPolicyResponse
        }

        public async getDownloadUrl(id: number): Promise<GetDownloadUrlResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/policies/${encodeURIComponent(id)}/download-url`)
            return await resp.json() as GetDownloadUrlResponse
        }

        public async getUploadUrl(params: GetUploadUrlRequest): Promise<GetUploadUrlResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/policies/upload-url`, JSON.stringify(params))
            return await resp.json() as GetUploadUrlResponse
        }

        public async list(params: ListPoliciesRequest): Promise<ListPoliciesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                category: params.category,
                limit:    params.limit === undefined ? undefined : String(params.limit),
                offset:   params.offset === undefined ? undefined : String(params.offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/policies`, undefined, {query})
            return await resp.json() as ListPoliciesResponse
        }

        public async processEmbedding(policy_id: number): Promise<ProcessEmbeddingResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/policies/${encodeURIComponent(policy_id)}/embed`)
            return await resp.json() as ProcessEmbeddingResponse
        }

        public async queryHistory(params: QueryHistoryRequest): Promise<QueryHistoryResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:     params.limit === undefined ? undefined : String(params.limit),
                offset:    params.offset === undefined ? undefined : String(params.offset),
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/policies/queries`, undefined, {query})
            return await resp.json() as QueryHistoryResponse
        }

        public async upload(params: CreatePolicyDocRequest): Promise<PolicyDoc> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/policies`, JSON.stringify(params))
            return await resp.json() as PolicyDoc
        }
    }
}

export namespace profile {
    export interface CreatePersonRequest {
        name: string
        email: string
        "service_number"?: string
        rank?: string
        "watch_unit"?: string
        phone?: string
        address?: string
    }

    export interface CreatePersonResponse {
        user: user.User
        profile: FirefighterProfile
    }

    export interface CreateProfileRequest {
        "user_id": string
        "service_number"?: string
        station?: string
        shift?: string
        watch?: WatchUnit
        rank?: string
        "hire_date"?: string
        phone?: string
        "emergency_contact_name"?: string
        "emergency_contact_phone"?: string
        skills?: string[]
        certifications?: string[]
        driver?: DriverQualifications
        driverPathway?: DriverPathway
        prps?: boolean
        ba?: boolean
        notes?: string
        "last_one_to_one_date"?: string
        "next_one_to_one_date"?: string
        lastConversation?: LastConversation
        customFields?: { [key: string]: string | number | boolean | null }
        "rolling_sick_episodes"?: number
        "rolling_sick_days"?: number
        "trigger_stage"?: TriggerStage
    }

    export interface DriverPathway {
        status: DriverPathwayStatus
        lgvPassedDate?: string
    }

    export type DriverPathwayStatus = "medical_due" | "application_sent" | "awaiting_theory" | "awaiting_course" | "passed_LGV" | "awaiting_ERD" | "passed"

    export interface DriverQualifications {
        lgv: boolean
        erd: boolean
    }

    export interface FirefighterProfile {
        id: number
        "user_id": string
        "service_number"?: string
        station?: string
        shift?: string
        watch?: WatchUnit
        rank?: string
        "hire_date"?: string
        phone?: string
        "emergency_contact_name"?: string
        "emergency_contact_phone"?: string
        skills?: string[]
        certifications?: string[]
        driver?: DriverQualifications
        driverPathway?: DriverPathway
        prps?: boolean
        ba?: boolean
        notes?: string
        "last_one_to_one_date"?: string
        "next_one_to_one_date"?: string
        lastConversation?: LastConversation
        customFields?: { [key: string]: string | number | boolean | null }
        "rolling_sick_episodes": number
        "rolling_sick_days": number
        "trigger_stage": TriggerStage
        "created_at": string
        "updated_at": string
    }

    export interface LastConversation {
        date: string
        text: string
    }

    export interface ListPeopleRequest {
        station?: string
        shift?: string
        rank?: string
        "watch_unit"?: string
        status?: "all" | "active" | "inactive"
        limit?: number
        offset?: number
    }

    export interface ListPeopleResponse {
        people: PersonWithProfile[]
        total: number
    }

    export interface ListProfilesRequest {
        station?: string
        shift?: string
        watch?: string
        limit?: number
        offset?: number
    }

    export interface ListProfilesResponse {
        profiles: FirefighterProfile[]
        total: number
    }

    export interface PersonWithProfile {
        user: user.User
        profile: FirefighterProfile | null
    }

    export interface RecalculateTriggersRequest {
        "user_id": string
    }

    export interface RecalculateTriggersResponse {
        "rolling_sick_episodes": number
        "rolling_sick_days": number
        "trigger_stage": TriggerStage
        "previous_trigger_stage": TriggerStage
        changed: boolean
    }

    export type TriggerStage = "None" | "Stage1" | "Stage2" | "Stage3"

    export type WatchUnit = "Green" | "Red" | "White" | "Blue" | "Amber"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.createPerson = this.createPerson.bind(this)
            this.getByUser = this.getByUser.bind(this)
            this.list = this.list.bind(this)
            this.listWithUsers = this.listWithUsers.bind(this)
            this.recalculateTriggers = this.recalculateTriggers.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateProfileRequest): Promise<FirefighterProfile> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/profiles`, JSON.stringify(params))
            return await resp.json() as FirefighterProfile
        }

        public async createPerson(params: CreatePersonRequest): Promise<CreatePersonResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/people`, JSON.stringify(params))
            return await resp.json() as CreatePersonResponse
        }

        public async getByUser(user_id: string): Promise<FirefighterProfile> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/profiles/user/${encodeURIComponent(user_id)}`)
            return await resp.json() as FirefighterProfile
        }

        public async list(params: ListProfilesRequest): Promise<ListProfilesResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:   params.limit === undefined ? undefined : String(params.limit),
                offset:  params.offset === undefined ? undefined : String(params.offset),
                shift:   params.shift,
                station: params.station,
                watch:   params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/profiles`, undefined, {query})
            return await resp.json() as ListProfilesResponse
        }

        public async listWithUsers(params: ListPeopleRequest): Promise<ListPeopleResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:        params.limit === undefined ? undefined : String(params.limit),
                offset:       params.offset === undefined ? undefined : String(params.offset),
                rank:         params.rank,
                shift:        params.shift,
                station:      params.station,
                status:       params.status === undefined ? undefined : String(params.status),
                "watch_unit": params["watch_unit"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/people`, undefined, {query})
            return await resp.json() as ListPeopleResponse
        }

        public async recalculateTriggers(params: RecalculateTriggersRequest): Promise<RecalculateTriggersResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/profile/recalculate-triggers`, JSON.stringify(params))
            return await resp.json() as RecalculateTriggersResponse
        }

        public async update(id: number, params: {
    "service_number"?: string
    station?: string
    shift?: string
    watch?: WatchUnit
    rank?: string
    "hire_date"?: string
    phone?: string
    "emergency_contact_name"?: string
    "emergency_contact_phone"?: string
    skills?: string[]
    certifications?: string[]
    driver?: DriverQualifications
    driverPathway?: DriverPathway
    prps?: boolean
    ba?: boolean
    notes?: string
    "last_one_to_one_date"?: string
    "next_one_to_one_date"?: string
    lastConversation?: LastConversation
    customFields?: { [key: string]: string | number | boolean | null }
    "rolling_sick_episodes"?: number
    "rolling_sick_days"?: number
    "trigger_stage"?: TriggerStage
}): Promise<FirefighterProfile> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/profiles/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as FirefighterProfile
        }
    }
}

export namespace quarterly_report {
    export interface AddCustomItemResponse {
        item: QuarterlyReportCustomItem
    }

    export interface CreateReportRequest {
        "station_name"?: string
        watch: string
        "watch_commander_name": string
        quarter: number
        "financial_year": number
    }

    export interface CreateReportResponse {
        report: QuarterlyReport
    }

    export interface GetReportResponse {
        report: QuarterlyReport
    }

    export interface ListReportsRequest {
        watch?: string
        "financial_year"?: number
        quarter?: number
    }

    export interface ListReportsResponse {
        reports: QuarterlyReportSummary[]
    }

    export interface QuarterlyReport {
        id: number
        "station_name": string
        watch: string
        "watch_commander_name": string
        quarter: number
        "financial_year": number
        notes?: string
        status: ReportStatus
        "created_by": string
        "created_at": string
        "updated_at": string
        items: QuarterlyReportItem[]
        "custom_items": QuarterlyReportCustomItem[]
    }

    export interface QuarterlyReportCustomItem {
        id: number
        "report_id": number
        description: string
        "target_text"?: string
        met: boolean
        rationale?: string
        "sort_order": number
        "created_at": string
        "updated_at": string
    }

    export interface QuarterlyReportItem {
        id: number
        "report_id": number
        "kpi_code": string
        description: string
        "target_text": string
        "row_color": string
        category: string
        "sort_order": number
        met: boolean
        "actual_value"?: string
        rationale?: string
        "created_at": string
        "updated_at": string
    }

    export interface QuarterlyReportSummary {
        id: number
        "station_name": string
        watch: string
        "watch_commander_name": string
        quarter: number
        "financial_year": number
        status: ReportStatus
        "items_total": number
        "items_met": number
        "created_at": string
        "updated_at": string
    }

    export type ReportStatus = "draft" | "submitted"

    export interface UpdateCustomItemResponse {
        item: QuarterlyReportCustomItem
    }

    export interface UpdateItemResponse {
        item: QuarterlyReportItem
    }

    export interface UpdateReportResponse {
        report: QuarterlyReport
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.addCustomItem = this.addCustomItem.bind(this)
            this.create = this.create.bind(this)
            this.deleteCustomItem = this.deleteCustomItem.bind(this)
            this.get = this.get.bind(this)
            this.list = this.list.bind(this)
            this.updateCustomItem = this.updateCustomItem.bind(this)
            this.updateItem = this.updateItem.bind(this)
            this.updateReport = this.updateReport.bind(this)
        }

        public async addCustomItem(id: number, params: {
    description: string
    "target_text"?: string
}): Promise<AddCustomItemResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/quarterly-reports/${encodeURIComponent(id)}/custom-items`, JSON.stringify(params))
            return await resp.json() as AddCustomItemResponse
        }

        public async create(params: CreateReportRequest): Promise<CreateReportResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/quarterly-reports`, JSON.stringify(params))
            return await resp.json() as CreateReportResponse
        }

        public async deleteCustomItem(id: number, itemId: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/quarterly-reports/${encodeURIComponent(id)}/custom-items/${encodeURIComponent(itemId)}`)
        }

        public async get(id: number): Promise<GetReportResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/quarterly-reports/${encodeURIComponent(id)}`)
            return await resp.json() as GetReportResponse
        }

        public async list(params: ListReportsRequest): Promise<ListReportsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "financial_year": params["financial_year"] === undefined ? undefined : String(params["financial_year"]),
                quarter:          params.quarter === undefined ? undefined : String(params.quarter),
                watch:            params.watch,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/quarterly-reports`, undefined, {query})
            return await resp.json() as ListReportsResponse
        }

        public async updateCustomItem(id: number, itemId: number, params: {
    description?: string
    "target_text"?: string
    met?: boolean
    rationale?: string
}): Promise<UpdateCustomItemResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/quarterly-reports/${encodeURIComponent(id)}/custom-items/${encodeURIComponent(itemId)}`, JSON.stringify(params))
            return await resp.json() as UpdateCustomItemResponse
        }

        public async updateItem(id: number, itemId: number, params: {
    met?: boolean
    "actual_value"?: string
    rationale?: string
    "target_text"?: string
}): Promise<UpdateItemResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/quarterly-reports/${encodeURIComponent(id)}/items/${encodeURIComponent(itemId)}`, JSON.stringify(params))
            return await resp.json() as UpdateItemResponse
        }

        public async updateReport(id: number, params: {
    notes?: string
    status?: ReportStatus
}): Promise<UpdateReportResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/quarterly-reports/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as UpdateReportResponse
        }
    }
}

export namespace report {
    export interface ExportCsvRequest {
        "user_id": string
    }

    export interface ExportCsvResponse {
        csv: string
    }

    export interface ExportTableCSVRequest {
        table: string
    }

    export interface ExportTableCSVResponse {
        csv: string
        filename: string
    }

    export interface ImportStaffCsvRequest {
        "csv_data": string
        "user_id": string
    }

    export interface ImportStaffCsvResponse {
        "imported_count": number
        errors: string[]
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.exportCsv = this.exportCsv.bind(this)
            this.exportTableCSV = this.exportTableCSV.bind(this)
            this.importStaffCsv = this.importStaffCsv.bind(this)
        }

        public async exportCsv(table: string, params: ExportCsvRequest): Promise<ExportCsvResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "user_id": params["user_id"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/reports/export/${encodeURIComponent(table)}`, undefined, {query})
            return await resp.json() as ExportCsvResponse
        }

        public async exportTableCSV(params: ExportTableCSVRequest): Promise<ExportTableCSVResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/report/export`, JSON.stringify(params))
            return await resp.json() as ExportTableCSVResponse
        }

        public async importStaffCsv(params: ImportStaffCsvRequest): Promise<ImportStaffCsvResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/reports/import-staff`, JSON.stringify(params))
            return await resp.json() as ImportStaffCsvResponse
        }
    }
}

export namespace settings {
    export interface FeatureFlags {
        dashboard: boolean
        people: boolean
        calendar: boolean
        tasks: boolean
        targets: boolean
        detachments: boolean
        equipment: boolean
        handover: boolean
        policies: boolean
        resources: boolean
        reports: boolean
        inspections: boolean
    }

    export interface SystemSettings {
        id: number
        "skills_dictionary": string[]
        "certifications_dictionary": string[]
        "absence_threshold_days": number
        "absence_threshold_period_months": number
        "trigger_stage1_episodes": number
        "trigger_stage1_days": number
        "trigger_stage2_episodes": number
        "trigger_stage2_days": number
        "trigger_stage3_episodes": number
        "trigger_stage3_days": number
        "branding_logo_url"?: string
        "branding_primary_color"?: string
        "branding_secondary_color"?: string
        "feature_flags": FeatureFlags
        "created_at": string
        "updated_at": string
    }

    export interface UpdateAbsenceThresholdsRequest {
        "absence_threshold_days"?: number
        "absence_threshold_period_months"?: number
    }

    export interface UpdateBrandingRequest {
        "branding_logo_url"?: string
        "branding_primary_color"?: string
        "branding_secondary_color"?: string
    }

    export interface UpdateFeatureFlagsRequest {
        "feature_flags": FeatureFlags
    }

    export interface UpdateSkillsCertsRequest {
        "skills_dictionary"?: string[]
        "certifications_dictionary"?: string[]
    }

    export interface UpdateTriggerThresholdsRequest {
        "trigger_stage1_episodes"?: number
        "trigger_stage1_days"?: number
        "trigger_stage2_episodes"?: number
        "trigger_stage2_days"?: number
        "trigger_stage3_episodes"?: number
        "trigger_stage3_days"?: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.get = this.get.bind(this)
            this.updateAbsenceThresholds = this.updateAbsenceThresholds.bind(this)
            this.updateBranding = this.updateBranding.bind(this)
            this.updateFeatureFlags = this.updateFeatureFlags.bind(this)
            this.updateSkillsCerts = this.updateSkillsCerts.bind(this)
            this.updateTriggerThresholds = this.updateTriggerThresholds.bind(this)
        }

        public async get(): Promise<SystemSettings> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/settings`)
            return await resp.json() as SystemSettings
        }

        public async updateAbsenceThresholds(params: UpdateAbsenceThresholdsRequest): Promise<SystemSettings> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/settings/absence-thresholds`, JSON.stringify(params))
            return await resp.json() as SystemSettings
        }

        public async updateBranding(params: UpdateBrandingRequest): Promise<SystemSettings> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/settings/branding`, JSON.stringify(params))
            return await resp.json() as SystemSettings
        }

        public async updateFeatureFlags(params: UpdateFeatureFlagsRequest): Promise<SystemSettings> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/settings/feature-flags`, JSON.stringify(params))
            return await resp.json() as SystemSettings
        }

        public async updateSkillsCerts(params: UpdateSkillsCertsRequest): Promise<SystemSettings> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/settings/skills-certs`, JSON.stringify(params))
            return await resp.json() as SystemSettings
        }

        public async updateTriggerThresholds(params: UpdateTriggerThresholdsRequest): Promise<SystemSettings> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/settings/trigger-thresholds`, JSON.stringify(params))
            return await resp.json() as SystemSettings
        }
    }
}

export namespace shift_adjustments {

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteAdjustment = this.deleteAdjustment.bind(this)
            this.list = this.list.bind(this)
        }

        public async create(params: shift_adjustments.CreateShiftAdjustmentRequest): Promise<shift_adjustments.ShiftAdjustment> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/shift-adjustments`, JSON.stringify(params))
            return await resp.json() as shift_adjustments.ShiftAdjustment
        }

        public async deleteAdjustment(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/shift-adjustments/${encodeURIComponent(id)}`)
        }

        public async list(params: shift_adjustments.ListShiftAdjustmentsRequest): Promise<shift_adjustments.ListShiftAdjustmentsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "covering_watch": params["covering_watch"],
                "end_date":       params["end_date"],
                "start_date":     params["start_date"],
                "user_id":        params["user_id"],
                "watch_unit":     params["watch_unit"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/shift-adjustments`, undefined, {query})
            return await resp.json() as shift_adjustments.ListShiftAdjustmentsResponse
        }
    }
}

export namespace skill {
    export interface CreateSkillRenewalRequest {
        "profile_id": number
        "skill_name": string
        "acquired_date"?: string
        "renewal_date"?: string
        "expiry_date"?: string
        "reminder_date"?: string
        notes?: string
    }

    export interface ExpiringSkillEntry {
        id: number
        "profile_id": number
        "user_id": string
        "user_name": string
        "skill_name": string
        "expiry_date"?: string
        status: "expired" | "warning"
        "days_until_expiry"?: number
    }

    export interface ListExpiringSkillsResponse {
        skills: ExpiringSkillEntry[]
        "expired_count": number
        "warning_count": number
    }

    export interface ListSkillRenewalsRequest {
        "profile_id": number
    }

    export interface ListSkillRenewalsResponse {
        skills: SkillRenewal[]
    }

    export interface SkillRenewal {
        id: number
        "profile_id": number
        "skill_name": string
        "acquired_date"?: string
        "renewal_date"?: string
        "expiry_date"?: string
        "reminder_date"?: string
        notes?: string
        status?: SkillStatus
        "days_until_expiry"?: number
        "days_until_reminder"?: number
        "created_at": string
        "updated_at": string
    }

    export type SkillStatus = "valid" | "warning" | "expired"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteSkillRenewal = this.deleteSkillRenewal.bind(this)
            this.list = this.list.bind(this)
            this.listExpiring = this.listExpiring.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateSkillRenewalRequest): Promise<SkillRenewal> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/skills/renewals`, JSON.stringify(params))
            return await resp.json() as SkillRenewal
        }

        public async deleteSkillRenewal(id: number): Promise<{
    success: boolean
}> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/skills/renewals/${encodeURIComponent(id)}`)
            return await resp.json() as {
    success: boolean
}
        }

        public async list(params: ListSkillRenewalsRequest): Promise<ListSkillRenewalsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "profile_id": String(params["profile_id"]),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/skills/renewals`, undefined, {query})
            return await resp.json() as ListSkillRenewalsResponse
        }

        public async listExpiring(): Promise<ListExpiringSkillsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/skills/renewals/expiring`)
            return await resp.json() as ListExpiringSkillsResponse
        }

        public async update(id: number, params: {
    "skill_name"?: string
    "acquired_date"?: string
    "renewal_date"?: string
    "expiry_date"?: string
    "reminder_date"?: string
    notes?: string
}): Promise<SkillRenewal> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/skills/renewals/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as SkillRenewal
        }
    }
}

export namespace targets {
    export interface CreateTargetRequest {
        "period_start": string
        "period_end": string
        metric: TargetMetric
        "target_count": number
        "actual_count"?: number
        notes?: string
        status?: TargetStatus
    }

    export interface ListTargetsRequest {
        metric?: TargetMetric
        status?: TargetStatus
        "period_start"?: string
        "period_end"?: string
        limit?: number
        offset?: number
    }

    export interface ListTargetsResponse {
        targets: Target[]
        total: number
    }

    export interface Target {
        id: number
        "period_start": string
        "period_end": string
        metric: TargetMetric
        "target_count": number
        "actual_count": number
        notes?: string
        status: TargetStatus
        "created_at": string
        "updated_at": string
    }

    export type TargetMetric = "HFSV" | "HighRise" | "Hydrants" | "Activities"

    export type TargetStatus = "active" | "completed" | "at_risk" | "overdue"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.list = this.list.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateTargetRequest): Promise<Target> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/targets`, JSON.stringify(params))
            return await resp.json() as Target
        }

        public async list(params: ListTargetsRequest): Promise<ListTargetsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:          params.limit === undefined ? undefined : String(params.limit),
                metric:         params.metric === undefined ? undefined : String(params.metric),
                offset:         params.offset === undefined ? undefined : String(params.offset),
                "period_end":   params["period_end"],
                "period_start": params["period_start"],
                status:         params.status === undefined ? undefined : String(params.status),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/targets`, undefined, {query})
            return await resp.json() as ListTargetsResponse
        }

        public async update(id: number, params: {
    "period_start"?: string
    "period_end"?: string
    metric?: TargetMetric
    "target_count"?: number
    "actual_count"?: number
    notes?: string
    status?: TargetStatus
}): Promise<Target> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/targets/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as Target
        }
    }
}

export namespace task {
    export interface ChecklistItem {
        label: string
        done: boolean
    }

    export interface CreateColumnRequest {
        name: string
        "status_key": string
        color?: string
    }

    export interface CreateTaskRequest {
        title: string
        description?: string
        category: TaskCategory
        "assigned_to"?: string
        "assigned_by": string
        "watch_unit"?: string
        priority?: TaskPriority
        "due_date"?: string
        checklist?: ChecklistItem[]
        attachments?: string[]
        rrule?: string
        tags?: string[]
        position?: number
        status?: TaskStatus
        "source_type"?: string
        "source_id"?: number
        "calendar_event_id"?: number
    }

    export interface CreateTemplateRequest {
        name: string
        description?: string
        "title_template": string
        "task_description"?: string
        category: TaskCategory
        priority: TaskPriority
        checklist?: ChecklistItem[]
        rrule: string
    }

    export interface ListColumnsResponse {
        columns: TaskColumn[]
    }

    export interface ListTasksRequest {
        "assigned_to"?: string
        "watch_unit"?: string
        "assigned_by"?: string
        status?: TaskStatus
        priority?: TaskPriority
        limit?: number
        offset?: number
    }

    export interface ListTasksResponse {
        tasks: Task[]
        total: number
    }

    export interface ListTemplatesResponse {
        templates: TaskTemplate[]
    }

    export interface ReorderColumnsRequest {
        columns: {
            id: number
            position: number
        }[]
    }

    export interface Task {
        id: number
        title: string
        description?: string
        category: TaskCategory
        "assigned_to_user_id"?: string
        "watch_unit"?: string
        status: TaskStatus
        priority: TaskPriority
        "due_at"?: string
        checklist?: ChecklistItem[]
        attachments?: string[]
        tags?: string[]
        rrule?: string
        position?: number
        "completed_at"?: string
        "cover_colour"?: string
        "source_type"?: string
        "source_id"?: number
        "calendar_event_id"?: number
        "created_at": string
        "updated_at": string
    }

    export type TaskCategory = "Training" | "Inspection" | "HFSV" | "Admin" | "Other"

    export interface TaskColumn {
        id: number
        name: string
        "status_key": string
        color: string
        position: number
        "created_at": string
    }

    export type TaskPriority = "Low" | "Med" | "High"

    export type TaskStatus = string | "InProgress" | "Blocked" | "Done"

    export interface TaskTemplate {
        id: number
        name: string
        description?: string
        "title_template": string
        "task_description"?: string
        category: TaskCategory
        priority: TaskPriority
        checklist?: ChecklistItem[]
        rrule: string
        "is_active": boolean
        "created_by": string
        "created_at": string
        "updated_at": string
    }

    export interface UpdateColumnRequest {
        name?: string
        color?: string
    }

    export interface UpdateTemplateRequest {
        name?: string
        description?: string
        "title_template"?: string
        "task_description"?: string
        category?: TaskCategory
        priority?: TaskPriority
        checklist?: ChecklistItem[]
        rrule?: string
        "is_active"?: boolean
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.createColumn = this.createColumn.bind(this)
            this.createTemplate = this.createTemplate.bind(this)
            this.deleteColumn = this.deleteColumn.bind(this)
            this.deleteTask = this.deleteTask.bind(this)
            this.deleteTemplate = this.deleteTemplate.bind(this)
            this.list = this.list.bind(this)
            this.listColumns = this.listColumns.bind(this)
            this.listTemplates = this.listTemplates.bind(this)
            this.reorderColumns = this.reorderColumns.bind(this)
            this.update = this.update.bind(this)
            this.updateColumn = this.updateColumn.bind(this)
            this.updateTemplate = this.updateTemplate.bind(this)
        }

        public async create(params: CreateTaskRequest): Promise<Task> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/tasks`, JSON.stringify(params))
            return await resp.json() as Task
        }

        public async createColumn(params: CreateColumnRequest): Promise<TaskColumn> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/task-columns`, JSON.stringify(params))
            return await resp.json() as TaskColumn
        }

        public async createTemplate(params: CreateTemplateRequest): Promise<TaskTemplate> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/task-templates`, JSON.stringify(params))
            return await resp.json() as TaskTemplate
        }

        public async deleteColumn(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/task-columns/${encodeURIComponent(id)}`)
        }

        public async deleteTask(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/tasks/${encodeURIComponent(id)}`)
        }

        public async deleteTemplate(id: number): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/task-templates/${encodeURIComponent(id)}`)
        }

        public async list(params: ListTasksRequest): Promise<ListTasksResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                "assigned_by": params["assigned_by"],
                "assigned_to": params["assigned_to"],
                limit:         params.limit === undefined ? undefined : String(params.limit),
                offset:        params.offset === undefined ? undefined : String(params.offset),
                priority:      params.priority === undefined ? undefined : String(params.priority),
                status:        params.status === undefined ? undefined : String(params.status),
                "watch_unit":  params["watch_unit"],
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/tasks`, undefined, {query})
            return await resp.json() as ListTasksResponse
        }

        public async listColumns(): Promise<ListColumnsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/task-columns`)
            return await resp.json() as ListColumnsResponse
        }

        public async listTemplates(): Promise<ListTemplatesResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/task-templates`)
            return await resp.json() as ListTemplatesResponse
        }

        public async reorderColumns(params: ReorderColumnsRequest): Promise<void> {
            await this.baseClient.callTypedAPI("POST", `/task-columns/reorder`, JSON.stringify(params))
        }

        public async update(id: number, params: {
    title?: string
    description?: string
    category?: TaskCategory
    "assigned_to"?: string
    "watch_unit"?: string
    status?: TaskStatus
    priority?: TaskPriority
    "due_date"?: string
    checklist?: ChecklistItem[]
    attachments?: string[]
    rrule?: string
    tags?: string[]
    "cover_colour"?: string
    position?: number
}): Promise<Task> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/tasks/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as Task
        }

        public async updateColumn(id: number, params: UpdateColumnRequest): Promise<TaskColumn> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/task-columns/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as TaskColumn
        }

        public async updateTemplate(id: number, params: UpdateTemplateRequest): Promise<TaskTemplate> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/task-templates/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as TaskTemplate
        }
    }
}

export namespace user {
    export interface CreateUserRequest {
        id: string
        email: string
        name: string
        role: string
        "watch_unit"?: string
        rank?: string
        "avatar_url"?: string
        "is_active"?: boolean
    }

    export interface ListBasicUsersResponse {
        users: {
            id: string
            name: string
            email: string
        }[]
    }

    export interface ListUsersRequest {
        role?: UserRole
        limit?: number
        offset?: number
    }

    export interface ListUsersResponse {
        users: User[]
        total: number
    }

    export interface UpdateUserParams {
        updates: UpdateUserRequest
    }

    export interface UpdateUserRequest {
        email?: string
        name?: string
        role?: string
        "watch_unit"?: string
        rank?: string
        "avatar_url"?: string
        "last_login_at"?: string
        "is_active"?: boolean
        "left_at"?: string
    }

    export interface User {
        id: string
        email: string
        name: string
        role: string
        "watch_unit"?: string
        rank?: string
        "avatar_url"?: string
        "last_login_at"?: string
        "is_active": boolean
        "is_admin": boolean
        "left_at"?: string
        "password_hash"?: string
        "password_reset_token"?: string
        "password_reset_expires"?: string
        "created_at": string
        "updated_at": string
    }

    export type UserRole = "WC" | "CC" | "FF" | "RO"

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.create = this.create.bind(this)
            this.deleteUser = this.deleteUser.bind(this)
            this.get = this.get.bind(this)
            this.list = this.list.bind(this)
            this.listBasic = this.listBasic.bind(this)
            this.update = this.update.bind(this)
        }

        public async create(params: CreateUserRequest): Promise<User> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/users`, JSON.stringify(params))
            return await resp.json() as User
        }

        public async deleteUser(id: string): Promise<void> {
            await this.baseClient.callTypedAPI("DELETE", `/users/${encodeURIComponent(id)}`)
        }

        public async get(id: string): Promise<User> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/${encodeURIComponent(id)}`)
            return await resp.json() as User
        }

        public async list(params: ListUsersRequest): Promise<ListUsersResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  params.limit === undefined ? undefined : String(params.limit),
                offset: params.offset === undefined ? undefined : String(params.offset),
                role:   params.role === undefined ? undefined : String(params.role),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users`, undefined, {query})
            return await resp.json() as ListUsersResponse
        }

        public async listBasic(): Promise<ListBasicUsersResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/users/basic`)
            return await resp.json() as ListBasicUsersResponse
        }

        public async update(id: string, params: UpdateUserParams): Promise<User> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/users/${encodeURIComponent(id)}`, JSON.stringify(params))
            return await resp.json() as User
        }
    }
}

export namespace shift_adjustments {
    export interface CreateShiftAdjustmentRequest {
        type: ShiftAdjustmentType
        "start_date": string
        "end_date": string
        "covering_user_id"?: string
        "covering_name"?: string
        "covering_watch"?: string
        "shift_day_night"?: "Day" | "Night"
        notes?: string
        "for_user_id"?: string
    }

    export interface ListShiftAdjustmentsRequest {
        "user_id"?: string
        "watch_unit"?: string
        "covering_watch"?: string
        "start_date"?: string
        "end_date"?: string
    }

    export interface ListShiftAdjustmentsResponse {
        adjustments: ShiftAdjustment[]
    }

    export interface ShiftAdjustment {
        id: number
        "user_id": string
        "user_name"?: string
        type: ShiftAdjustmentType
        "start_date": string
        "end_date": string
        "covering_user_id"?: string
        "covering_name"?: string
        "covering_watch"?: string
        "shift_day_night"?: "Day" | "Night"
        "watch_unit": string
        notes?: string
        "created_by_user_id": string
        "created_at": string
        "updated_at": string
    }

    export type ShiftAdjustmentType = "flexi" | "training" | "h4h" | "flexi_payback" | "orange_day"
}



function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "method" | "body" | "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}

// AuthDataGenerator is a function that returns a new instance of the authentication data required by this API
export type AuthDataGenerator = () =>
  | auth.AuthParams
  | Promise<auth.AuthParams | undefined>
  | undefined;

// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = (input: RequestInfo | URL, init?: RequestInit) => globalThis.fetch(input, init);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
    readonly authGenerator?: AuthDataGenerator

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "watch-commander-ops-hub-8spi-Generated-TS-Client (Encore/v1.54.0)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }

        // Setup an authentication data generator using the auth data token option
        if (options.auth !== undefined) {
            const auth = options.auth
            if (typeof auth === "function") {
                this.authGenerator = auth
            } else {
                this.authGenerator = () => auth
            }
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        let authData: auth.AuthParams | undefined;

        // If authorization data generator is present, call it and add the returned data to the request
        if (this.authGenerator) {
            const mayBePromise = this.authGenerator();
            if (mayBePromise instanceof Promise) {
                authData = await mayBePromise;
            } else {
                authData = mayBePromise;
            }
        }

        if (authData) {
            const data: CallParameters = {};

            data.headers = makeRecord<string, string>({
                authorization: authData.authorization,
            });

            return data;
        }

        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        return this.callAPI(method, path, body, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
            method,
            body: body ?? null,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}
