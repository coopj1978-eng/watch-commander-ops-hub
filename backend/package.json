{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && npx vite build --outDir=../backend/frontend/dist",
    "test": "vitest"
  },
  "dependencies": {
    "@clerk/backend": "^1.27.0",
    "bcrypt": "^5.1.1",
    "encore.dev": "^1.54.0",
    "jsonwebtoken": "^9.0.2"
  },
  "devDependencies": {
    "@clerk/backend": "^1.27.0",
    "typescript": "^5.8.3",
    "vitest": "^3.0.9"
  }
}